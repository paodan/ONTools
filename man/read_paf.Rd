% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_paf.R
\name{read_paf}
\alias{read_paf}
\title{Read a PAF file into a data frame}
\usage{
read_paf(paf_file, parse_tags = TRUE, simplify = TRUE)
}
\arguments{
\item{paf_file}{Path to a \code{.paf} or \code{.paf.gz} file.}

\item{parse_tags}{Logical. If \code{TRUE}, split optional \code{TAG:TYPE:VALUE} fields
into separate columns named by \code{TAG}.}

\item{simplify}{Logical. If \code{TRUE}, convert standard numeric PAF columns and
parsed integer/float tags to numeric columns when possible.}
}
\value{
A data frame, or \code{NULL} for empty PAF files. Standard PAF columns are
named \code{query_name}, \code{query_length}, \code{query_start}, \code{query_end}, \code{strand},
\code{target_name}, \code{target_length}, \code{target_start}, \code{target_end},
\code{residue_matches}, \code{alignment_block_length}, and \code{mapping_quality}.
}
\description{
\code{read_paf()} reads a plain or gzipped PAF file and returns a data frame with
standard PAF column names. Optional SAM-style tags can be kept as raw fields
or parsed into separate columns.
}
\examples{
paf <- tempfile(fileext = ".paf")
writeLines(
  "read1\t1000\t0\t900\t+\tchr1\t2000\t100\t1000\t890\t900\t60\tNM:i:10",
  paf
)

read_paf(paf)

}
