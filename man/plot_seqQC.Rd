% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_seqQC.R
\name{plot_seqQC}
\alias{plot_seqQC}
\title{Plot sequencing-summary QC for an ONT run}
\usage{
plot_seqQC(
  filePath,
  runName = NULL,
  device = "pdf",
  unclassified = "unclassified",
  barcodes = 1:96,
  barcode_digits = 2,
  out_dir = "figs",
  sample_col = "alias",
  length_col = "sequence_length_template",
  min_read_length = NULL,
  max_read_length = NULL,
  filename_suffix = NULL
)
}
\arguments{
\item{filePath}{Path to a tab-delimited sequencing summary file.}

\item{runName}{Run name used in output filenames. When \code{NULL}, the input file
basename without extension is used.}

\item{device}{Graphics device for saving plots. Supported values are
\code{"pdf"}, \code{"png"}, \code{"svg"}, \code{"jpg"}, and \code{"jpeg"}. Use \code{NULL} to return
plots without writing files.}

\item{unclassified}{Label used for unclassified reads.}

\item{barcodes}{Integer barcode numbers to include and order. Use \code{NULL} to
use sample labels as they appear in the sequencing summary file.}

\item{barcode_digits}{Minimum number of digits used for barcode labels.}

\item{out_dir}{Parent output directory for plot files.}

\item{sample_col}{Column containing barcode or sample labels. Defaults to
\code{"alias"}, matching the existing ONTools workflow.}

\item{length_col}{Column containing read lengths. Defaults to
\code{"sequence_length_template"}.}

\item{min_read_length}{Optional minimum read length to keep before plotting.}

\item{max_read_length}{Optional maximum read length to keep before plotting.}

\item{filename_suffix}{Optional suffix added to output filenames before the
file extension. When \code{NULL}, a suffix is generated from length filters, for
example \code{"len1200-1800"}.}
}
\value{
Invisibly returns a list with the read-count plot (\code{numRead}), the
read-length plot (\code{lenRead}), demultiplexing recovery percentage
(\code{recovery}), per-sample read counts (\code{read_counts}), total reads before
filtering (\code{n_reads_raw}), total reads after filtering (\code{n_reads_filtered}),
filters applied (\code{filters}), and paths written (\code{files}).
}
\description{
\code{plot_seqQC()} reads a MinKNOW or dorado \code{sequencing_summary*.txt} file and
creates read-count and read-length QC plots by barcode or sample alias.
}
\examples{
summary_file <- tempfile(fileext = ".txt")
write.table(
  data.frame(
    alias = c("barcode01", "barcode01", "unclassified"),
    sequence_length_template = c(1200, 800, 500)
  ),
  summary_file,
  sep = "\t",
  row.names = FALSE,
  quote = FALSE
)

qc <- plot_seqQC(summary_file, runName = "example", device = NULL, barcodes = 1)
qc$recovery

filtered_qc <- plot_seqQC(
  summary_file,
  runName = "example_filtered",
  device = NULL,
  barcodes = 1,
  min_read_length = 1000,
  max_read_length = 1800
)
filtered_qc$n_reads_filtered

}
