% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dehost_fastq.R
\name{dehost_fastq}
\alias{dehost_fastq}
\title{Remove host/reference-like reads from a FASTQ file}
\usage{
dehost_fastq(
  reference,
  fastq,
  output_fastq = NULL,
  out_dir = "work/dehost",
  prefix = NULL,
  threads = 16,
  min_mapq = 20,
  min_aln_frac = 0.8,
  minimap2 = "minimap2",
  seqkit = "seqkit",
  conda_env = NULL,
  conda = "conda",
  dry_run = FALSE,
  echo = TRUE,
  stdout = "",
  stderr = ""
)
}
\arguments{
\item{reference}{Reference FASTA file.}

\item{fastq}{Input FASTQ or FASTQ.GZ file.}

\item{output_fastq}{Output FASTQ or FASTQ.GZ file. If \code{NULL}, writes
\verb{<out_dir>/<input_basename>.dehost.fastq.gz}.}

\item{out_dir}{Output directory for intermediate files and, when
\code{output_fastq = NULL}, the filtered FASTQ file.}

\item{prefix}{Prefix for intermediate PAF, read id, and stats files.}

\item{threads}{Positive integer thread count passed to \code{minimap2}.}

\item{min_mapq}{Minimum PAF mapping quality. Default: 20.}

\item{min_aln_frac}{Minimum aligned fraction, calculated as PAF column 11
divided by read length in PAF column 2. Default: 0.8.}

\item{minimap2, seqkit}{Command names or executable paths.}

\item{conda_env}{Optional conda environment name. If supplied, external
commands are run with \verb{conda run -n <conda_env>}.}

\item{conda}{Conda executable name or path used when \code{conda_env} is supplied.}

\item{dry_run}{Logical. If \code{TRUE}, return planned commands without running.}

\item{echo}{Logical. If \code{TRUE}, print planned commands before execution.}

\item{stdout, stderr}{Passed to \code{\link[=system2]{system2()}} for commands that stream to the R
console. File-generating commands use explicit output paths.}
}
\value{
Invisibly returns a list with \code{status}, \code{commands}, and \code{paths}.
}
\description{
\code{dehost_fastq()} maps reads to a reference with \code{minimap2}, identifies
reference-like reads by PAF mapping quality and aligned fraction, then writes
a FASTQ file with those reads removed.
}
\examples{
ref <- tempfile(fileext = ".fasta")
reads <- tempfile(fileext = ".fastq.gz")
writeLines(c(">ecoli", "ACGTACGT"), ref)
res <- dehost_fastq(ref, reads, dry_run = TRUE)
res$commands$minimap2

}
