% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bamsnap_snapshot.R
\name{bamsnap_snapshot}
\alias{bamsnap_snapshot}
\title{Export a BamSnap alignment snapshot from R}
\usage{
bamsnap_snapshot(
  genome_fasta,
  bam,
  chr,
  start = NULL,
  end = NULL,
  out_dir = "alignment",
  format = "png",
  snapshot_name = NULL,
  bamsnap = "bamsnap",
  title = NULL,
  draw = c("coordinates", "bamplot", "base"),
  bamplot = c("coverage", "base", "read"),
  width = 1000,
  height = NULL,
  margin = NULL,
  no_title = FALSE,
  no_target_line = FALSE,
  save_image_only = TRUE,
  process = 1L,
  extra_args = NULL,
  auto_index_fasta = TRUE,
  dry_run = FALSE,
  quiet = TRUE,
  overwrite = TRUE
)
}
\arguments{
\item{genome_fasta}{Path to the reference FASTA file passed to BamSnap.}

\item{bam}{Path to the coordinate-sorted BAM file passed to BamSnap.}

\item{chr}{Reference sequence, chromosome, or contig name.}

\item{start}{Optional start coordinate of the region. When both \code{start} and
\code{end} are \code{NULL}, \code{chr} is passed as the whole target region.}

\item{end}{Optional end coordinate of the region. When both \code{start} and
\code{end} are \code{NULL}, \code{chr} is passed as the whole target region.}

\item{out_dir}{Directory where the snapshot image is written.}

\item{format}{Snapshot output format. Supported values are \code{"png"} and
\code{"jpg"}.}

\item{snapshot_name}{Output snapshot filename. The extension should match
\code{format}. When \code{NULL}, a filename is generated from \code{chr}, \code{start}, \code{end},
and \code{format}.}

\item{bamsnap}{Command or full path used to launch BamSnap.}

\item{title}{Optional label shown for the BAM track.}

\item{draw}{Character vector of BamSnap tracks passed after \code{-draw}.}

\item{bamplot}{Character vector of BAM subtracks passed after \code{-bamplot}.}

\item{width, height}{Optional image width and height in pixels.}

\item{margin}{Optional genomic margin size passed to BamSnap.}

\item{no_title}{Logical. If \code{TRUE}, add \code{-no_title}.}

\item{no_target_line}{Logical. If \code{TRUE}, add \code{-no_target_line}.}

\item{save_image_only}{Logical. If \code{TRUE}, add \code{-save_image_only}.}

\item{process}{Number of BamSnap worker processes.}

\item{extra_args}{Character vector of additional raw BamSnap arguments.}

\item{auto_index_fasta}{Logical. If \code{TRUE}, create a missing FASTA \code{.fai}
index with \code{\link[Rsamtools:scanFa]{Rsamtools::indexFa()}} before running BamSnap.}

\item{dry_run}{Logical. If \code{TRUE}, return the command without launching
BamSnap.}

\item{quiet}{Logical. If \code{TRUE}, suppress console output from FASTA indexing
and the BamSnap system command.}

\item{overwrite}{Logical. If \code{FALSE}, stop when the output snapshot already
exists.}
}
\value{
Invisibly returns a list containing the command, arguments, exit
status, region, output snapshot path, and FASTA indexing metadata.
}
\description{
\code{bamsnap_snapshot()} runs the command-line BamSnap viewer for a reference
FASTA, BAM file, genomic region, and output image. Unlike IGV Desktop, this
does not require opening a graphical application.
}
\details{
BamSnap must be installed separately, for example with \verb{pip install bamsnap}.
BAM files should be coordinate sorted and indexed with a standard index name
such as \code{sample.bam.bai}.
}
\examples{
ref <- tempfile(fileext = ".fasta")
bam <- tempfile(fileext = ".bam")
writeLines(c(">barcode09", "ACGTACGTACGT"), ref)
file.create(bam)
file.create(paste0(bam, ".bai"))

res <- bamsnap_snapshot(
  genome_fasta = ref,
  bam = bam,
  chr = "barcode09",
  start = 1,
  end = 12,
  dry_run = TRUE
)
res$args

}
