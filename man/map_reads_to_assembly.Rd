% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_reads_to_assembly.R
\name{map_reads_to_assembly}
\alias{map_reads_to_assembly}
\title{Map reads back to an assembly and plot read depth}
\usage{
map_reads_to_assembly(
  assembly,
  reads,
  align_bam,
  depth_file = NULL,
  depth_plot = NULL,
  preset = "map-ont",
  threads = 16,
  secondary = TRUE,
  depth_all_positions = FALSE,
  depth_all_references = FALSE,
  min_mapping_quality = NULL,
  min_base_quality = NULL,
  plot_depth = TRUE,
  plot_width = 8,
  plot_height = 5,
  facet_nrow = 3,
  minimap2 = "minimap2",
  samtools = "samtools",
  conda_env = NULL,
  conda = "conda",
  dry_run = FALSE,
  echo = TRUE,
  stderr = ""
)
}
\arguments{
\item{assembly}{Assembly FASTA file used as the mapping reference.}

\item{reads}{Input reads in FASTQ or FASTQ.GZ format.}

\item{align_bam}{Output coordinate-sorted BAM file.}

\item{depth_file}{Output depth table. If \code{NULL}, writes
\verb{<align_bam without .bam>.depth.txt}.}

\item{depth_plot}{Output depth plot image. If \code{NULL} and \code{plot_depth = TRUE},
writes \verb{<depth_file>.png}.}

\item{preset}{Minimap2 preset passed to \code{minimap2 -x}. Default \code{"map-ont"}
matches Nanopore reads.}

\item{threads}{Positive integer thread count passed to \code{minimap2 -t} and
\code{samtools sort -@}.}

\item{secondary}{Logical. If \code{FALSE}, pass \code{--secondary=no} to minimap2.}

\item{depth_all_positions}{Logical. If \code{TRUE}, pass \code{samtools depth -a} so
zero-depth positions are also reported.}

\item{depth_all_references}{Logical. If \code{TRUE}, pass \code{samtools depth -aa} so
every position in every reference sequence is reported. This overrides
\code{depth_all_positions}.}

\item{min_mapping_quality}{Optional minimum mapping quality passed to
\code{samtools depth -Q}.}

\item{min_base_quality}{Optional minimum base quality passed to
\code{samtools depth -q}.}

\item{plot_depth}{Logical. If \code{TRUE}, read the depth table and save a depth
plot with ggplot2.}

\item{plot_width, plot_height}{Width and height in inches passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} when \code{plot_depth = TRUE}.}

\item{facet_nrow}{Number of rows passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} for contig
panels in the depth plot.}

\item{minimap2, samtools}{Command names or executable paths.}

\item{conda_env}{Optional conda environment name. If supplied, external
commands are run with \verb{conda run -n <conda_env>}.}

\item{conda}{Conda executable name or path used when \code{conda_env} is supplied.}

\item{dry_run}{Logical. If \code{TRUE}, return planned commands without running.}

\item{echo}{Logical. If \code{TRUE}, print planned commands before execution.}

\item{stderr}{Passed to \code{\link[=system2]{system2()}}. Defaults stream errors to the R console.}
}
\value{
Invisibly returns a list with \code{status}, \code{commands}, \code{paths}, \code{plot},
\code{preset}, and \code{conda_env}.
}
\description{
\code{map_reads_to_assembly()} maps Nanopore reads back to assembled contigs with
\code{minimap2}, sorts and indexes the BAM with \code{samtools}, writes per-base depth
with \code{samtools depth}, and optionally saves a depth plot.
}
\examples{
assembly <- tempfile(fileext = ".fasta")
reads <- tempfile(fileext = ".fastq")
bam <- tempfile(fileext = ".bam")
writeLines(c(">contig1", "ACGTACGT"), assembly)
writeLines(c("@read1", "ACGT", "+", "!!!!"), reads)
res <- map_reads_to_assembly(assembly, reads, bam, dry_run = TRUE)
res$commands$minimap2

}
