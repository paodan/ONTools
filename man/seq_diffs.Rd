% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_diffs.R
\name{seq_diffs}
\alias{seq_diffs}
\title{Compare two DNA sequences and report differences}
\usage{
seq_diffs(seqs, type = "global", ...)
}
\arguments{
\item{seqs}{A character vector containing exactly two DNA sequences. Names are
optional but will be used in the output when present.}

\item{type}{Alignment type passed to \code{\link[pwalign:pairwiseAlignment]{pwalign::pairwiseAlignment()}}. Defaults
to \code{"global"}.}

\item{...}{Additional arguments passed to \code{\link[pwalign:pairwiseAlignment]{pwalign::pairwiseAlignment()}}.}
}
\value{
A list with:
\describe{
\item{orientation}{Best orientation for the second sequence: \code{"forward"} or
\code{"reverse_complement"}.}
\item{n_diff}{Number of aligned positions where the two sequences differ.}
\item{differences}{A data frame containing difference positions, bases,
sequence names, and orientation metadata.}
\item{alignment}{The selected \code{PairwiseAlignmentsSingleSubject} object.}
\item{aligned_seq1}{The aligned first sequence, including gap characters.}
\item{aligned_seq2}{The aligned second sequence in the selected orientation,
including gap characters.}
}
}
\description{
\code{seq_diffs()} aligns two DNA sequences in both forward and reverse-complement
orientations, chooses the orientation with fewer aligned differences, and
reports mismatch, insertion, and deletion positions.
}
\examples{
seqs <- c(
  A = "ATGCCGTAAA",
  B = "TTACGGCATAA"
)

res <- seq_diffs(seqs)
res$orientation
res$differences

}
