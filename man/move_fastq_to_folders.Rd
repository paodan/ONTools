% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/move_fastq_to_folders.R
\name{move_fastq_to_folders}
\alias{move_fastq_to_folders}
\title{Move demultiplexed FASTQ barcode folders into project groups}
\usage{
move_fastq_to_folders(
  fastq_dir,
  sample_info,
  project_col = NULL,
  amplicon_size_col = "Expected_Amplicon_Size_bp",
  barcode_col = "barcode",
  overwrite = FALSE,
  dry_run = FALSE
)
}
\arguments{
\item{fastq_dir}{Directory containing demultiplexed \verb{barcode*} folders.}

\item{sample_info}{Sample information table, either a data frame or a CSV
file path.}

\item{project_col}{Optional column in \code{sample_info} identifying the project.
When \code{NULL}, output groups are based only on \code{amplicon_size_col}.}

\item{amplicon_size_col}{Column in \code{sample_info} identifying expected amplicon
size or length group.}

\item{barcode_col}{Column in \code{sample_info} identifying barcode folder names.}

\item{overwrite}{Logical. If \code{FALSE}, stop when a destination barcode folder
already exists.}

\item{dry_run}{Logical. If \code{TRUE}, return the move plan without moving files.}
}
\value{
Invisibly returns a data frame with one row per barcode in
\code{sample_info}, including the output group, source path, destination path,
source/destination existence, and move status.
}
\description{
\code{move_fastq_to_folders()} reorganizes \verb{barcode*} folders from one FASTQ
directory into subfolders defined by a sample information table. This is
useful when one sequencing run contains amplicons from multiple projects or
multiple expected amplicon lengths.
}
\examples{
fastq_dir <- tempfile("fastq-pass-trim-")
dir.create(file.path(fastq_dir, "barcode01"), recursive = TRUE)
dir.create(file.path(fastq_dir, "barcode02"), recursive = TRUE)
sample_info <- data.frame(
  barcode = c("barcode01", "barcode02"),
  Project = c("Project A", "Project B"),
  Expected_Amplicon_Size_bp = c("3500bp", "1600bp")
)

move_fastq_to_folders(
  fastq_dir = fastq_dir,
  sample_info = sample_info,
  project_col = "Project",
  dry_run = TRUE
)

}
