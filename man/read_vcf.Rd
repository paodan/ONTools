% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_vcf.R
\name{read_vcf}
\alias{read_vcf}
\title{Read a VCF file into a data frame}
\usage{
read_vcf(
  vcf_file,
  parse_info = TRUE,
  parse_genotypes = TRUE,
  sample_format = c("wide", "long"),
  samples = NULL,
  add_variant_type = TRUE,
  keep_info = FALSE,
  keep_format = FALSE,
  simplify = TRUE,
  info_prefix = "",
  genotype_prefix = NULL
)
}
\arguments{
\item{vcf_file}{Path to a \code{.vcf} or \code{.vcf.gz} file.}

\item{parse_info}{Logical. If \code{TRUE}, split the \code{INFO} column into separate
columns.}

\item{parse_genotypes}{Logical. If \code{TRUE}, split sample columns according to
the \code{FORMAT} column.}

\item{sample_format}{Output shape for parsed sample genotype fields.
\code{"wide"} keeps one row per VCF record; \code{"long"} returns one row per
VCF-record/sample pair.}

\item{samples}{Optional character vector of sample names to keep. If \code{NULL},
all VCF sample columns are used.}

\item{add_variant_type}{Logical. If \code{TRUE}, add a \code{Type} column inferred from
the \code{REF} and \code{ALT} alleles. The rule mirrors the variant type shown in the
wf-amplicon report with an extra normalized-indel guard: single-base
substitutions are \code{SNP}, unequal-length alleles with a shared anchor base
are \code{INDEL}, equal-length multi-base substitutions are \code{MNP}, unequal-length
alleles without a shared anchor are \code{OTHER}, and reference/missing calls are
\code{REF}/\code{NA}.}

\item{keep_info}{Logical. If \code{TRUE}, keep the original \code{INFO} column after
parsing.}

\item{keep_format}{Logical. If \code{TRUE}, keep the original \code{FORMAT} and raw
sample genotype columns after parsing.}

\item{simplify}{Logical. If \code{TRUE}, convert parsed scalar numeric fields to
numeric columns when possible.}

\item{info_prefix}{Prefix added to parsed \code{INFO} column names.}

\item{genotype_prefix}{Prefix added to parsed genotype field names. When
\code{NULL}, single-sample wide output uses bare names such as \code{GT} and \code{DP};
multi-sample wide output uses \code{<sample>_<field>}.}
}
\value{
A data frame. Empty VCF files return a zero-row data frame with the detected
VCF columns.
}
\description{
\code{read_vcf()} reads plain or gzipped VCF files and returns a data frame with
fixed VCF columns, optional REF/ALT variant types, parsed \code{INFO} fields, and
optionally parsed sample genotype fields from \code{FORMAT}.
}
\examples{
vcf <- tempfile(fileext = ".vcf")
writeLines(c(
  "##fileformat=VCFv4.2",
  "#CHROM\tPOS\tID\tREF\tALT\tQUAL\tFILTER\tINFO\tFORMAT\tsample1",
  "chr1\t10\t.\tA\tG\t60\tPASS\tDP=30;AF=0.4\tGT:DP:AD\t0/1:30:18,12"
), vcf)

read_vcf(vcf)
}
