% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_circular_consensus_delivery.R
\name{make_circular_consensus_delivery}
\alias{make_circular_consensus_delivery}
\title{Build a circular consensus delivery package}
\usage{
make_circular_consensus_delivery(
  consensus,
  output,
  project,
  sequence_type = c("circular_dna", "plasmid", "virus"),
  sample_sheet = NULL,
  bam = NULL,
  bai = NULL,
  depth = NULL,
  variants_vcf = NULL,
  variants_vcf_index = NULL,
  variants_af = NULL,
  variants_af_filtered = NULL,
  assembly_info = NULL,
  flye_log = NULL,
  notes = NULL,
  overwrite = FALSE,
  script = NULL,
  bash = "bash",
  dry_run = FALSE,
  echo = TRUE,
  wait = TRUE,
  stdout = "",
  stderr = ""
)
}
\arguments{
\item{consensus}{Final consensus FASTA file.}

\item{output}{Output root directory. The shell script creates
\verb{<output>/<project>_consensus_delivery/} and
\verb{<output>/<project>_consensus_delivery.tar.gz}.}

\item{project}{Project identifier used in the delivery directory and archive
names.}

\item{sequence_type}{Sequence type label passed to \code{--sequence-type}. Must be
one of \code{"circular_dna"}, \code{"plasmid"}, or \code{"virus"}.}

\item{sample_sheet}{Optional sample sheet or project metadata file to copy
into \verb{00_metadata/}.}

\item{bam}{Optional final alignment BAM file to copy into \verb{02_evidence/}.}

\item{bai}{Optional BAM index file to copy into \verb{02_evidence/}.}

\item{depth}{Optional depth file to copy into \verb{02_evidence/}.}

\item{variants_vcf}{Optional variant VCF file, usually \code{variants.vcf.gz},
to copy into \verb{03_qc/}.}

\item{variants_vcf_index}{Optional variant VCF index file, usually
\code{variants.vcf.gz.csi}, to copy into \verb{03_qc/}.}

\item{variants_af}{Optional variant allele-frequency table to copy into
\verb{03_qc/}.}

\item{variants_af_filtered}{Optional filtered variant allele-frequency table,
for example \code{variants_gt0.05.af.tsv}, to copy into \verb{03_qc/}.}

\item{assembly_info}{Optional Flye \code{assembly_info.txt} file to copy into
\verb{02_evidence/}.}

\item{flye_log}{Optional Flye log file to copy into \verb{02_evidence/}.}

\item{notes}{Optional notes text file to copy into \verb{00_metadata/}.}

\item{overwrite}{Logical. If \code{TRUE}, remove an existing
\verb{<output>/<project>_consensus_delivery/} directory or archive before
rebuilding. Defaults to \code{FALSE} to protect existing delivery packages.}

\item{script}{Path to the shell script. Defaults to the script bundled with
ONTools.}

\item{bash}{Bash executable used to run \code{script}.}

\item{dry_run}{Logical. If \code{TRUE}, return the command without running it.}

\item{echo}{Logical. If \code{TRUE}, print the command before execution.}

\item{wait}{Logical. Passed to \code{\link[=system2]{system2()}}. Use \code{FALSE} to launch the command
asynchronously.}

\item{stdout, stderr}{Passed to \code{\link[=system2]{system2()}}. Defaults stream output to the R
console.}
}
\value{
Invisibly returns a list with \code{command}, \code{args}, \code{command_string},
\code{execution_command}, \code{execution_args}, \code{status}, and \code{paths}. In
\code{dry_run = TRUE}, \code{status} is \code{NA_integer_}.
}
\description{
\code{make_circular_consensus_delivery()} is an R wrapper around the bundled
\code{make_circular_consensus_delivery.sh} shell script. It packages a finished
ONT circular DNA consensus FASTA together with optional evidence files,
writes README and manifest files, generates MD5 checksums, and creates a
\code{.tar.gz} archive.
}
\details{
The wrapped shell script copies the supplied consensus sequence as-is. It
does not annotate, polish, rotate, rename FASTA headers, or otherwise modify
sequence contents.

External command-line software must be installed separately before running a
real delivery build.

Required:
\itemize{
\item \code{seqkit}
\item \code{tar}
\item \code{gzip}
\item an MD5 command: \code{md5sum} on Linux, or \code{md5} on macOS
}

Optional:
\itemize{
\item \code{samtools}, used to generate \verb{01_consensus/consensus.fasta.fai}
}

The easiest isolated installation is usually via conda/mamba:
\verb{mamba create -n ont-delivery -c conda-forge -c bioconda seqkit samtools}
followed by \verb{conda activate ont-delivery}.

On macOS with Homebrew, install the core tools with
\verb{brew install seqkit samtools}. \code{tar}, \code{gzip}, and \code{md5} are normally already
available on macOS.
}
\examples{
consensus <- tempfile(fileext = ".fasta")
output_dir <- tempfile("consensus-delivery-")
dir.create(output_dir)
writeLines(c(">consensus1", "ACGTACGTACGT"), consensus)

res <- make_circular_consensus_delivery(
  consensus = consensus,
  output = output_dir,
  project = "PROJECT001",
  sequence_type = "plasmid",
  dry_run = TRUE
)
res$command_string

}
