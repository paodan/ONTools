% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_vcf.R
\name{vcf_variant_type}
\alias{vcf_variant_type}
\title{Infer VCF variant type from REF and ALT alleles}
\usage{
vcf_variant_type(ref, alt, collapse_multiallelic = TRUE)
}
\arguments{
\item{ref}{Character vector of VCF \code{REF} alleles.}

\item{alt}{Character vector of VCF \code{ALT} alleles. Multi-allelic values such as
\code{"A,AT"} are supported.}

\item{collapse_multiallelic}{Logical. If \code{TRUE}, a multi-allelic \code{ALT} value
is summarized into one value per VCF row. Rows with mixed non-reference
variant types are reported as \code{"MIXED"}. If \code{FALSE}, a list-column with one
type per ALT allele is returned.}
}
\value{
A character vector when \code{collapse_multiallelic = TRUE}; otherwise a
list of character vectors.
}
\description{
\code{vcf_variant_type()} classifies variants from the allele strings in the same
broad way used by wf-amplicon reports, with an extra normalized-indel guard:
single-base substitutions are \code{SNP}, unequal-length alleles with a shared
anchor base are \code{INDEL}, equal-length multi-base substitutions are \code{MNP},
unequal-length alleles without a shared anchor are \code{OTHER}, and reference or
missing calls are \code{REF} or \code{NA}.
}
\examples{
vcf_variant_type(c("A", "AT", "A", "AC"), c("G", "A", "AT", "GT"))
}
