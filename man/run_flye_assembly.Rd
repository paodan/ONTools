% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_flye_assembly.R
\name{run_flye_assembly}
\alias{run_flye_assembly}
\title{Assemble long reads with Flye}
\usage{
run_flye_assembly(
  reads,
  out_dir,
  genome_size,
  read_type = c("nano_raw", "nano_hq", "nano_corr", "pacbio_raw", "pacbio_corr",
    "pacbio_hifi"),
  min_overlap = NULL,
  threads = 1,
  iterations = NULL,
  meta = FALSE,
  plasmids = FALSE,
  trestle = FALSE,
  keep_haplotypes = FALSE,
  no_alt_contigs = FALSE,
  polisher = NULL,
  read_error = NULL,
  extra_args = NULL,
  flye = "flye",
  conda_env = NULL,
  conda = "conda",
  dry_run = FALSE,
  echo = TRUE,
  wait = TRUE,
  stdout = "",
  stderr = ""
)
}
\arguments{
\item{reads}{Input read file. For Nanopore workflows this is usually a FASTQ
or FASTQ.GZ file after optional host removal and length filtering.}

\item{out_dir}{Output directory passed to \code{flye --out-dir}. Flye writes its
assembly files, logs, and intermediate files here.}

\item{genome_size}{Expected genome or plasmid size passed to
\code{flye --genome-size}, for example \code{"180k"}, \code{"5m"}, or \code{"3.1g"}.}

\item{read_type}{Read mode used by Flye. \code{"nano_raw"} passes \code{--nano-raw};
\code{"nano_hq"} passes \code{--nano-hq}; \code{"nano_corr"} passes \code{--nano-corr};
\code{"pacbio_raw"} passes \code{--pacbio-raw}; \code{"pacbio_corr"} passes
\code{--pacbio-corr}; \code{"pacbio_hifi"} passes \code{--pacbio-hifi}.}

\item{min_overlap}{Optional minimum overlap between reads, passed to
\code{flye --min-overlap}. Larger values can reduce spurious overlaps but may
require longer reads.}

\item{threads}{Number of CPU threads passed to \code{flye --threads}.}

\item{iterations}{Optional number of polishing iterations passed to
\code{flye --iterations}.}

\item{meta}{Logical. If \code{TRUE}, pass \code{--meta} for metagenome or uneven
coverage assembly.}

\item{plasmids}{Logical. If \code{TRUE}, pass \code{--plasmids} to recover plasmids
from isolate assemblies.}

\item{trestle}{Logical. If \code{TRUE}, pass \code{--trestle} to use Flye's Trestle
repeat resolver when available in the installed Flye version.}

\item{keep_haplotypes}{Logical. If \code{TRUE}, pass \code{--keep-haplotypes} to keep
alternative haplotypes instead of collapsing them.}

\item{no_alt_contigs}{Logical. If \code{TRUE}, pass \code{--no-alt-contigs} to skip
output of alternative contigs.}

\item{polisher}{Optional polisher name passed to \code{flye --polisher}. Supported
values can depend on the installed Flye version.}

\item{read_error}{Optional expected per-base read error rate passed to
\code{flye --read-error}, for example \code{0.03}.}

\item{extra_args}{Optional character vector of additional raw Flye arguments
appended to the command.}

\item{flye}{Command name or executable path for \code{flye}.}

\item{conda_env}{Optional conda environment name. If supplied, Flye is run
with \verb{conda run -n <conda_env>}.}

\item{conda}{Conda executable name or path used when \code{conda_env} is supplied.}

\item{dry_run}{Logical. If \code{TRUE}, return the planned command without
running it.}

\item{echo}{Logical. If \code{TRUE}, print the planned command before execution.}

\item{wait, stdout, stderr}{Passed to \code{\link[=system2]{system2()}}. Defaults stream Flye output
to the R console and wait for completion.}
}
\value{
Invisibly returns a list with \code{status}, \code{command}, \code{args},
\code{command_string}, \code{paths}, \code{read_type}, and \code{conda_env}. \code{paths} includes
the input reads, output directory, and common Flye output files:
\code{assembly.fasta}, \code{assembly_info.txt}, and \code{flye.log}.
}
\description{
\code{run_flye_assembly()} wraps \code{flye} for long-read assembly. By default it
matches a Nanopore raw-read plasmid assembly workflow.
}
\examples{
reads <- tempfile(fileext = ".fastq")
writeLines(c("@read1", "ACGT", "+", "!!!!"), reads)
res <- run_flye_assembly(
  reads = reads,
  genome_size = "180k",
  min_overlap = 3000,
  threads = 22,
  out_dir = tempfile("flye-"),
  dry_run = TRUE
)
res$command_string

}
