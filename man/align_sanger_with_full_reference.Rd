% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align_sanger_with_full_reference.R
\name{align_sanger_with_full_reference}
\alias{align_sanger_with_full_reference}
\title{Align Sanger reads to a full-length reference}
\usage{
align_sanger_with_full_reference(
  reference,
  sanger,
  match_score = 2,
  mismatch_score = -3,
  gap_opening = 5,
  gap_extension = 2,
  processors = 1
)
}
\arguments{
\item{reference}{Full-length reference sequence. Must be a FASTA file path or
a \code{\link[Biostrings:XStringSet-class]{Biostrings::DNAStringSet()}} containing exactly one sequence.}

\item{sanger}{Sanger read sequences. Must be a FASTA file path or a
\code{\link[Biostrings:XStringSet-class]{Biostrings::DNAStringSet()}} containing one or more sequences.}

\item{match_score, mismatch_score}{Numeric match and mismatch scores used to
build the nucleotide substitution matrix passed to
\code{\link[pwalign:pairwiseAlignment]{pwalign::pairwiseAlignment()}}.}

\item{gap_opening, gap_extension}{Numeric gap opening and extension penalties
passed to \code{\link[pwalign:pairwiseAlignment]{pwalign::pairwiseAlignment()}}.}

\item{processors}{Positive integer passed to \code{\link[DECIPHER:AlignSeqs]{DECIPHER::AlignSeqs()}}.}
}
\value{
A list with class \code{"SangerMultipleAlignment"} containing:
\describe{
\item{alignment}{Multiple sequence alignment returned by
\code{\link[DECIPHER:AlignSeqs]{DECIPHER::AlignSeqs()}}. The reference is the first sequence.}
\item{orientation}{A data frame with one row per Sanger read and columns
\code{sample}, \code{direction}, \code{forward_score}, \code{reverse_score},
\code{selected_score}, and \code{score_delta}.}
\item{oriented_sanger}{A \code{\link[Biostrings:XStringSet-class]{Biostrings::DNAStringSet()}} containing the Sanger
reads in the orientation selected for alignment.}
}
}
\description{
\code{align_sanger_with_full_reference()} orients Sanger reads against a single
full-length reference sequence, then returns a multiple sequence alignment
containing the reference followed by the oriented Sanger reads.
}
\details{
Each Sanger read is first aligned to the reference in both its original and
reverse-complement orientations using local pairwise alignment. The
orientation with the higher alignment score is retained. The reference and
oriented Sanger reads are then aligned together with
\code{\link[DECIPHER:AlignSeqs]{DECIPHER::AlignSeqs()}}.
}
\examples{
reference <- Biostrings::DNAStringSet(c(
  reference = "AAACCCGGGTTTAAACCCGGGTTT"
))
sanger <- Biostrings::DNAStringSet(c(
  read_forward = "CCCGGGTTTAAA",
  read_reverse = "TTTAAACCCGGG"
))

res <- align_sanger_with_full_reference(reference, sanger)
res$orientation
names(res$alignment)

}
