% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_sanger_alignment_pdf.R
\name{export_sanger_alignment_pdf}
\alias{export_sanger_alignment_pdf}
\title{Export a Sanger/reference alignment to HTML and PDF}
\usage{
export_sanger_alignment_pdf(
  alignment,
  pdf_file = "sanger_alignment.pdf",
  html_file = NULL,
  block_width = 80,
  highlight_reference = FALSE,
  render_pdf = TRUE,
  open_pdf = FALSE,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{alignment}{A \code{\link[Biostrings:XStringSet-class]{Biostrings::XStringSet()}} alignment object, or a
\code{"SangerMultipleAlignment"} list containing an \code{alignment} element.}

\item{pdf_file}{Output PDF path. Ignored when \code{render_pdf = FALSE}, except
for deriving \code{html_file} when \code{html_file = NULL}.}

\item{html_file}{Output HTML path. When \code{NULL}, it is derived from
\code{pdf_file} by replacing a trailing \code{.pdf} extension with \code{.html}.}

\item{block_width}{Positive integer column width passed to
\code{\link[DECIPHER:BrowseSeqs]{DECIPHER::BrowseSeqs()}}. It must be a multiple of 20.}

\item{highlight_reference}{Logical. If \code{TRUE}, highlight the first sequence,
which is expected to be the reference sequence.}

\item{render_pdf}{Logical. If \code{TRUE}, convert the HTML output to PDF with
\code{\link[pagedown:chrome_print]{pagedown::chrome_print()}}. This requires the suggested \code{pagedown} package
and an available Chromium/Chrome installation.}

\item{open_pdf}{Logical. If \code{TRUE} and \code{render_pdf = TRUE}, open the PDF after
it is written. Defaults to \code{FALSE} for non-interactive and batch use.}

\item{overwrite}{Logical. If \code{FALSE}, stop when an output file already exists.}

\item{quiet}{Logical. If \code{FALSE}, print output paths after rendering.}
}
\value{
Invisibly returns a list with \code{html}, \code{pdf}, \code{render_pdf}, and
\code{alignment_names}.
}
\description{
\code{export_sanger_alignment_pdf()} renders a Sanger/reference multiple sequence
alignment as a color-coded HTML file with \code{\link[DECIPHER:BrowseSeqs]{DECIPHER::BrowseSeqs()}}, and can
optionally convert that HTML file to PDF with \code{\link[pagedown:chrome_print]{pagedown::chrome_print()}}.
}
\details{
The \code{alignment} input can be the \code{alignment} element returned by
\code{\link[=align_sanger_with_full_reference]{align_sanger_with_full_reference()}}, or the full
\code{"SangerMultipleAlignment"} result object returned by that function.
}
\examples{
alignment <- Biostrings::DNAStringSet(c(
  reference = "AAACCCGGGTTT",
  read1 = "AAACCCGGGTTT",
  read2 = "AAA---GGGTTT"
))
html_file <- tempfile(fileext = ".html")

res <- export_sanger_alignment_pdf(
  alignment,
  html_file = html_file,
  render_pdf = FALSE
)
res$html

}
