% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_wf_amplicon.R
\name{run_wf_amplicon}
\alias{run_wf_amplicon}
\title{Run the wf-amplicon Nextflow workflow}
\usage{
run_wf_amplicon(
  fastq = "./fastq_pass_trim",
  out_dir = "./results/wf_amplicon_denovo",
  min_read_length = 2000,
  max_read_length = 3300,
  min_read_qual = 10,
  min_n_reads = 40,
  force_spoa_length_threshold = 2000,
  override_basecaller_cfg = "dna_r10.4.1_e8.2_400bps_sup@v5.2.0",
  profile = "standard",
  resume = TRUE,
  workflow = "julibeg/wf-amplicon",
  nextflow = "nextflow",
  extra_args = NULL,
  syntax_parser = "v1",
  nextflow_env = NULL,
  dry_run = FALSE,
  echo = TRUE,
  wait = TRUE,
  stdout = "",
  stderr = ""
)
}
\arguments{
\item{fastq}{Path to the input FASTQ directory passed to \code{--fastq}.}

\item{out_dir}{Output directory passed to \code{--out_dir}.}

\item{min_read_length}{Minimum read length passed to \code{--min_read_length}.}

\item{max_read_length}{Maximum read length passed to \code{--max_read_length}.}

\item{min_read_qual}{Minimum read quality passed to \code{--min_read_qual}.}

\item{min_n_reads}{Minimum number of reads passed to \code{--min_n_reads}.}

\item{force_spoa_length_threshold}{Value passed to
\code{--force_spoa_length_threshold}.}

\item{override_basecaller_cfg}{Basecaller configuration passed to
\code{--override_basecaller_cfg}.}

\item{profile}{Nextflow profile passed with \code{-profile}.}

\item{resume}{Logical. If \code{TRUE}, append \code{-resume}.}

\item{workflow}{Nextflow workflow name or path.}

\item{nextflow}{Nextflow executable name or path.}

\item{extra_args}{Optional raw command-line string appended after the standard
arguments. Use this for additional wf-amplicon parameters exactly as you
would type them in the shell.}

\item{syntax_parser}{Nextflow syntax parser version passed as the
\code{NXF_SYNTAX_PARSER} environment variable. Defaults to \code{"v1"} because
\code{julibeg/wf-amplicon} currently uses legacy Nextflow syntax that can fail
with newer Nextflow releases. Use \code{NULL} to leave the environment unchanged.}

\item{nextflow_env}{Optional character vector of additional environment
variables passed to \code{\link[=system2]{system2()}}, formatted as \code{"NAME=value"}.}

\item{dry_run}{Logical. If \code{TRUE}, return the command without running it.}

\item{echo}{Logical. If \code{TRUE}, print the command before execution.}

\item{wait}{Logical. Passed to \code{\link[=system2]{system2()}}. Use \code{FALSE} to launch the command
asynchronously.}

\item{stdout, stderr}{Passed to \code{\link[=system2]{system2()}}. Defaults stream output to the R
console.}
}
\value{
Invisibly returns a list with \code{command}, \code{args}, \code{extra_args},
\code{command_string}, \code{execution_command}, \code{execution_args}, \code{uses_shell},
\code{env}, \code{status}, and \code{paths}.
}
\description{
\code{run_wf_amplicon()} is an R wrapper around \verb{nextflow run julibeg/wf-amplicon} for ONT amplicon consensus analysis. Common parameters
are exposed as R arguments, and additional wf-amplicon options can be supplied
as a raw command-line string through \code{extra_args}.
}
\examples{
res <- run_wf_amplicon(
  fastq = "./fastq_pass_trim",
  out_dir = "./results/wf_amplicon_denovo",
  min_read_length = 2000,
  max_read_length = 3300,
  dry_run = TRUE
)
res$command_string

res2 <- run_wf_amplicon(
  fastq = "./fastq_pass_trim",
  out_dir = "./results/wf_amplicon_denovo",
  extra_args = "--threads 16 --some_param 'raw value'",
  dry_run = TRUE
)
res2$command_string

}
