% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dorado_steps.R
\name{dorado_demux_bam}
\alias{dorado_demux_bam}
\title{Demultiplex a Dorado BAM file into barcode BAM folders}
\usage{
dorado_demux_bam(
  calls_bam,
  demux_dir,
  kit_name = "EXP-NBD196",
  barcode_both_ends = TRUE,
  emit_summary = TRUE,
  threads = NULL,
  require_bam_pass = TRUE,
  barcode_config_dir = NULL,
  dorado = "dorado",
  conda_env = NULL,
  conda = "conda",
  dry_run = FALSE,
  echo = TRUE,
  stderr = ""
)
}
\arguments{
\item{calls_bam}{Input BAM generated by Dorado basecalling.}

\item{demux_dir}{Output directory passed to \code{dorado demux --output-dir}.}

\item{kit_name}{Dorado barcode kit name. Use \code{"YS-NB576"} for the ONTools
built-in 576-sample custom barcode set.}

\item{barcode_both_ends}{Logical. If \code{TRUE}, pass \code{--barcode-both-ends}.}

\item{emit_summary}{Logical. If \code{TRUE}, pass \code{--emit-summary}.}

\item{threads}{Optional thread count passed to \code{dorado demux --threads}.}

\item{require_bam_pass}{Logical. If \code{TRUE}, stop when no demultiplexed BAM
files are found under \code{demux_dir} after Dorado finishes.}

\item{barcode_config_dir}{Optional directory containing custom barcode
configuration files. If \code{NULL}, uses \code{ONTOOLS_BARCODE_CONFIG_DIR} or the
package bundled \code{docs/barcode_configs} directory.}

\item{dorado}{Command name or executable path for \code{dorado}.}

\item{conda_env}{Optional conda environment name. If supplied, Dorado is run
with \verb{conda run -n <conda_env>}.}

\item{conda}{Conda executable name or path used when \code{conda_env} is supplied.}

\item{dry_run}{Logical. If \code{TRUE}, return planned command without running.}

\item{echo}{Logical. If \code{TRUE}, print planned command before execution.}

\item{stderr}{Passed to \code{\link[=system2]{system2()}}. Defaults stream errors to the R console.}
}
\value{
Invisibly returns a list with \code{status}, \code{command}, \code{args},
\code{command_string}, \code{paths}, and \code{conda_env}.
}
\description{
\code{dorado_demux_bam()} runs \code{dorado demux} on a basecalled BAM file and writes
demultiplexed BAM files under a Dorado output directory.
}
