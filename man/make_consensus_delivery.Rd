% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_consensus_delivery.R
\name{make_consensus_delivery}
\alias{make_consensus_delivery}
\title{Build amplicon consensus delivery packages}
\usage{
make_consensus_delivery(
  path_proj,
  path_sampleInfo_file_list,
  path_delivery = "/data/project_delivery",
  kit_name = "YS-NB576",
  model = "sup",
  demux_out = NULL,
  fastq_out = "fastq_pass_trim",
  barcode_both_ends = FALSE,
  run_basecalling_demux_step = TRUE,
  run_QC_step = TRUE,
  move_fastq_step = TRUE,
  move_fastq_mode = c("move", "reuse", "auto"),
  run_amplicon_step = TRUE,
  trim_consensus_step = TRUE,
  run_filtered_QC_step = TRUE,
  run_igv_step = TRUE,
  collect_results_step = TRUE,
  make_ab1 = TRUE,
  ab1_name_template = "{barcode}.synthetic.ab1",
  ab1_samtools = "samtools",
  project_col = "Project_ID",
  amplicon_size_col = "Expected_Size_bp",
  barcode_col = "Barcode_ID",
  min_read_length_col = "Min_Read_Length",
  max_read_length_col = "Max_Read_Length",
  f_primer_col = "Primer_F",
  r_primer_col = "Primer_R",
  min_read_qual = 10,
  min_n_reads = 40,
  force_spoa_length_threshold = 2000,
  override_basecaller_cfg = "dna_r10.4.1_e8.2_400bps_sup@v5.2.0",
  profile = "standard",
  resume = TRUE,
  barcode_digits = 3,
  igv = "/usr/local/bin/IGV_Linux_2.19.8/igv.sh",
  overwrite_fastq = FALSE,
  overwrite_delivery = TRUE,
  include_execution = FALSE,
  dry_run = FALSE,
  echo = TRUE,
  wait = TRUE,
  stdout = "",
  stderr = ""
)
}
\arguments{
\item{path_proj}{MinKNOW project directory.}

\item{path_sampleInfo_file_list}{Named character vector or list of sample
information CSV files. Names are used as grouped FASTQ folder names.}

\item{path_delivery}{Root directory where per-group final results are copied.}

\item{kit_name}{Dorado barcode kit name.}

\item{model}{Dorado basecalling model or model alias.}

\item{demux_out}{Demux output directory name under \code{path_proj}. If \code{NULL},
defaults to \verb{demux_out_<kit_name>}.}

\item{fastq_out}{FASTQ output directory name created by Dorado conversion.}

\item{barcode_both_ends}{Logical. Passed to \code{\link[=run_dorado_demux_to_fastq]{run_dorado_demux_to_fastq()}}.}

\item{run_basecalling_demux_step}{Logical. If \code{FALSE}, reuse
\code{work/stat.Rdata} when present or reconstruct output paths from
\code{path_proj}, \code{demux_out}, and \code{fastq_out}.}

\item{run_QC_step}{Logical. Generate run-level QC plots and source FASTQ
statistics.}

\item{move_fastq_step}{Logical. Move barcode FASTQ folders into sample-info
groups before running amplicon analysis.}

\item{move_fastq_mode}{FASTQ grouping mode used when \code{move_fastq_step} is
\code{TRUE}. See \strong{FASTQ grouping and reruns} for the detailed behavior of
\code{"move"}, \code{"reuse"}, and \code{"auto"}.}

\item{run_amplicon_step}{Logical. Run \code{\link[=run_wf_amplicon]{run_wf_amplicon()}} for each group.}

\item{trim_consensus_step}{Logical. Trim consensus FASTA files when primer
columns are present and non-empty.}

\item{run_filtered_QC_step}{Logical. Generate QC plots for filtered reads for
each group.}

\item{run_igv_step}{Logical. Generate IGV snapshots for per-barcode BAM files.}

\item{collect_results_step}{Logical. Copy final deliverables with
\code{\link[=collect_amplicon_results]{collect_amplicon_results()}}.}

\item{make_ab1}{Logical. If \code{TRUE}, create synthetic AB1 chromatogram files
in the delivered \verb{barcode*} directories after result collection.}

\item{ab1_name_template}{Output AB1 filename template. \code{{barcode}} is
replaced with each barcode directory name.}

\item{ab1_samtools}{Command name or full path used by
\code{\link[=synthetic_ab1_from_bam]{synthetic_ab1_from_bam()}} to launch \code{samtools}.}

\item{project_col, amplicon_size_col, barcode_col}{Column names in each sample
information file.}

\item{min_read_length_col, max_read_length_col}{Primer-independent read-length
filter column names in each sample information file.}

\item{f_primer_col, r_primer_col}{Forward and reverse primer column names.}

\item{min_read_qual, min_n_reads, force_spoa_length_threshold, override_basecaller_cfg, profile, resume}{Parameters passed to \code{\link[=run_wf_amplicon]{run_wf_amplicon()}}.}

\item{barcode_digits}{Number of barcode digits used by \code{\link[=plot_seqQC]{plot_seqQC()}} for
filtered-read QC.}

\item{igv}{IGV executable path passed to \code{\link[=igv_snapshot]{igv_snapshot()}}.}

\item{overwrite_fastq}{Logical. Passed to \code{\link[=move_fastq_to_folders]{move_fastq_to_folders()}}.}

\item{overwrite_delivery}{Logical. Passed to \code{\link[=collect_amplicon_results]{collect_amplicon_results()}}.}

\item{include_execution}{Logical. Passed to \code{\link[=collect_amplicon_results]{collect_amplicon_results()}}.}

\item{dry_run}{Logical. If \code{TRUE}, build plans and commands without running
external analysis steps or copying final deliverables.}

\item{echo}{Logical. If \code{TRUE}, print commands from wrapped command runners.}

\item{wait}{Logical. Passed to wrapped command runners.}

\item{stdout, stderr}{Passed to wrapped command runners.}
}
\value{
Invisibly returns a list with run-level QC (\code{g1}), per-group QC
(\code{g2}), Dorado/basecalling status (\code{stat}), FASTQ move plans, amplicon
workflow results, trim summaries, IGV results, delivery copy summaries, AB1
generation summaries, and important output paths.
}
\description{
\code{make_consensus_delivery()} runs the common ONT amplicon consensus delivery
workflow: optional Dorado basecalling and demultiplexing, run-level QC,
grouping demultiplexed barcode FASTQ folders by sample information, running
\code{wf-amplicon}, optional primer-boundary trimming, filtered-read QC, optional
IGV snapshots, and final result collection.
}
\details{
\subsection{FASTQ grouping and reruns}{

\code{make_consensus_delivery()} expects each amplicon group to be available under
\verb{fastq_pass_trim/<group>/barcode*} before \code{wf-amplicon} is run. When
\code{move_fastq_step = TRUE}, \code{move_fastq_mode} controls how that grouped FASTQ
directory is prepared:
\itemize{
\item \code{"move"} keeps the original behavior. Barcode folders are moved from
\verb{fastq_pass_trim/barcode*} into \verb{fastq_pass_trim/<group>/barcode*} by
\code{\link[=move_fastq_to_folders]{move_fastq_to_folders()}}. With \code{overwrite_fastq = FALSE}, an existing
destination barcode folder stops the function, which protects previous
results from accidental replacement. This is best for a first run after a
fresh demultiplexing step.
\item \code{"reuse"} never moves barcode folders. It checks whether every barcode
listed in the sample information table already exists in
\verb{fastq_pass_trim/<group>/barcode*}. If any expected barcode folder is
missing, the function stops, except during \code{dry_run = TRUE}, where the
missing paths are returned in the move plan with
\code{status = "missing_destination"}. This is best when you deliberately want
to rerun only downstream analysis from a previously grouped FASTQ folder.
\item \code{"auto"} first performs the same destination check as \code{"reuse"}. If all
expected grouped barcode folders are already present, they are reused and
the move plan reports \code{status = "reused"}. If the grouped folder is not
complete, the function falls back to \code{"move"} and attempts to move barcode
folders from the ungrouped FASTQ directory. This is the recommended mode for
routine reruns because it handles both fresh and already-grouped projects.
}

If barcode folders were already moved in a previous run, rerun with either
\code{move_fastq_mode = "auto"} or \code{move_fastq_mode = "reuse"}. Keeping the
default \code{"move"} mode in that situation can fail because the source
\verb{fastq_pass_trim/barcode*} folders are no longer present and the destination
\verb{fastq_pass_trim/<group>/barcode*} folders already exist.

Setting \code{move_fastq_step = FALSE} skips this preparation entirely and assumes
the grouped FASTQ directory already exists.
}
}
\examples{
proj <- tempfile("ont-project-")
dir.create(file.path(proj, "pod5"), recursive = TRUE)
sample_info <- tempfile(fileext = ".csv")
utils::write.csv(
  data.frame(
    Barcode_ID = "PBC001-001",
    Project_ID = "PROJECT001",
    Expected_Size_bp = "1600",
    Min_Read_Length = 1200,
    Max_Read_Length = 1800
  ),
  sample_info,
  row.names = FALSE
)

res <- make_consensus_delivery(
  path_proj = proj,
  path_sampleInfo_file_list = c(PROJECT001_1600 = sample_info),
  dry_run = TRUE,
  echo = FALSE
)
res$workflow$PROJECT001_1600$command_string

}
