% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_ont_fastq_delivery.R
\name{make_ont_fastq_delivery}
\alias{make_ont_fastq_delivery}
\title{Build an ONT FASTQ delivery package}
\usage{
make_ont_fastq_delivery(
  input,
  output,
  project,
  sample_sheet = NULL,
  threads = 8,
  run_nanoplot = TRUE,
  run_multiqc = TRUE,
  script = NULL,
  bash = "bash",
  dry_run = FALSE,
  echo = TRUE,
  wait = TRUE,
  stdout = "",
  stderr = ""
)
}
\arguments{
\item{input}{Directory containing demultiplexed FASTQ files. Files ending in
\code{.fastq}, \code{.fq}, \code{.fastq.gz}, or \code{.fq.gz} are collected recursively.}

\item{output}{Output root directory. The shell script creates
\verb{<output>/<project>_delivery/} and \verb{<output>/<project>_delivery.tar.gz}.}

\item{project}{Project identifier used in the delivery directory and archive
names.}

\item{sample_sheet}{Optional sample sheet or barcode/sample metadata file to
copy into \verb{00_sample_sheet/}.}

\item{threads}{Positive integer thread count passed to NanoPlot.}

\item{run_nanoplot}{Logical. If \code{TRUE}, run NanoPlot for each delivered FASTQ
file. Set to \code{FALSE} to pass \code{--skip-nanoplot}.}

\item{run_multiqc}{Logical. If \code{TRUE}, run MultiQC on the QC output
directory. Set to \code{FALSE} to pass \code{--skip-multiqc}.}

\item{script}{Path to the shell script. Defaults to the script bundled with
ONTools.}

\item{bash}{Bash executable used to run \code{script}.}

\item{dry_run}{Logical. If \code{TRUE}, return the command without running it.}

\item{echo}{Logical. If \code{TRUE}, print the command before execution.}

\item{wait}{Logical. Passed to \code{\link[=system2]{system2()}}. Use \code{FALSE} to launch the command
asynchronously.}

\item{stdout, stderr}{Passed to \code{\link[=system2]{system2()}}. Defaults stream output to the R
console.}
}
\value{
Invisibly returns a list with \code{command}, \code{args}, \code{command_string},
\code{execution_command}, \code{execution_args}, \code{status}, and \code{paths}. In
\code{dry_run = TRUE}, \code{status} is \code{NA_integer_}.
}
\description{
\code{make_ont_fastq_delivery()} is an R wrapper around the bundled
\code{make_ont_fastq_delivery.sh} shell script. It copies demultiplexed ONT FASTQ
files into a customer-facing delivery directory, generates delivery-level QC
outputs, writes MD5 checksums, and creates a \code{.tar.gz} archive.
}
\details{
The wrapped shell script does not filter, rename, or modify FASTQ contents.

External command-line software must be installed separately before running a
real delivery build.

Required:
\itemize{
\item \code{seqkit}
\item \code{tar}
\item \code{gzip}
\item an MD5 command: \code{md5sum} on Linux, or \code{md5} on macOS
}

Recommended, and enabled by default:
\itemize{
\item \code{NanoPlot}
\item \code{multiqc}
}

The easiest isolated installation is usually via conda/mamba:
\verb{mamba create -n ont-delivery -c conda-forge -c bioconda seqkit nanoplot multiqc}
followed by \verb{conda activate ont-delivery}.

On macOS with Homebrew, install the core command-line tools with
\verb{brew install seqkit coreutils}. \code{tar}, \code{gzip}, and \code{md5} are normally already
available on macOS. Install the Python QC tools with
\verb{python3 -m pip install NanoPlot multiqc}, preferably inside a virtual
environment.

If \code{NanoPlot} or \code{multiqc} are not installed, set \code{run_nanoplot = FALSE} or
\code{run_multiqc = FALSE}.
}
\examples{
fastq_dir <- tempfile("ont-fastq-")
output_dir <- tempfile("ont-delivery-")
dir.create(fastq_dir)
dir.create(output_dir)
writeLines(c("@read1", "ACGT", "+", "!!!!"), file.path(fastq_dir, "sample.fastq"))

res <- make_ont_fastq_delivery(
  input = fastq_dir,
  output = output_dir,
  project = "PROJECT001",
  run_nanoplot = FALSE,
  run_multiqc = FALSE,
  dry_run = TRUE
)
res$command_string

}
