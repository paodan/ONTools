% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_consensus.R
\name{compare_consensus}
\alias{compare_consensus}
\title{Compare consensus sequences between two ONT runs}
\usage{
compare_consensus(
  consensus1,
  consensus2,
  sampleMap = NULL,
  type = "global",
  ...
)
}
\arguments{
\item{consensus1, consensus2}{Consensus sequences. Each input can be a path to
a FASTA file or a \code{DNAStringSet} object.}

\item{sampleMap}{Optional data frame defining which sequences to compare. The
first two columns are used as sequence names in \code{consensus1} and
\code{consensus2}, respectively. Columns named \code{seq1_name} and \code{seq2_name} are
preferred when present. When \code{NULL}, sequences with names present in both
inputs are compared.}

\item{type}{Alignment type passed to \code{\link[=seq_diffs]{seq_diffs()}}. Defaults to \code{"global"}.}

\item{...}{Additional arguments passed to \code{\link[=seq_diffs]{seq_diffs()}}.}
}
\value{
A data frame with one row per matched consensus pair and columns:
\describe{
\item{seq1_name, seq2_name}{Sequence names compared from \code{consensus1} and
\code{consensus2}.}
\item{distance}{Number of aligned differences returned by \code{\link[=seq_diffs]{seq_diffs()}}.}
\item{seq1_len, seq2_len}{Ungapped sequence lengths.}
\item{seq2_orientation}{Selected orientation for the second sequence.}
\item{seq1, seq2}{Original sequence strings.}
\item{aligned_seq1, aligned_seq2}{Aligned sequence strings including gaps.}
}
}
\description{
\code{compare_consensus()} compares matched consensus sequences from two FASTA
files or two \code{\link[Biostrings:XStringSet-class]{Biostrings::DNAStringSet()}} objects. Each matched pair is
aligned with \code{\link[=seq_diffs]{seq_diffs()}}, allowing the second sequence to be used in either
its original or reverse-complement orientation.
}
\examples{
consensus_a <- Biostrings::DNAStringSet(c(
  barcode01 = "ATGCCGTAAA",
  barcode02 = "AACCGGTT"
))
consensus_b <- Biostrings::DNAStringSet(c(
  barcode01 = "ATGCCGGTAAAC",
  barcode02 = "AACCGGTT"
))

compare_consensus(consensus_a, consensus_b)

sample_map <- data.frame(
  seq1_name = c("barcode01", "barcode02"),
  seq2_name = c("barcode02", "barcode01")
)
compare_consensus(consensus_a, consensus_b, sampleMap = sample_map)

}
