% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igv_snapshot.R
\name{igv_snapshot}
\alias{igv_snapshot}
\title{Export an IGV alignment snapshot from R}
\usage{
igv_snapshot(
  genome_fasta,
  bam,
  chr,
  start = NULL,
  end = NULL,
  out_dir = "alignment",
  format = "png",
  snapshot_name = NULL,
  igv = "igv.sh",
  batch_file = NULL,
  sort = "base",
  display = "collapse",
  max_panel_height = NULL,
  index = NULL,
  auto_index_fasta = TRUE,
  extra_commands = NULL,
  use_xvfb = FALSE,
  dry_run = FALSE,
  quiet = TRUE,
  overwrite = TRUE
)
}
\arguments{
\item{genome_fasta}{Path to the reference FASTA file loaded by IGV.}

\item{bam}{Path to the coordinate-sorted BAM file loaded by IGV.}

\item{chr}{Reference sequence, chromosome, or contig name passed to IGV.}

\item{start}{Optional start coordinate of the region passed to IGV. When
both \code{start} and \code{end} are \code{NULL}, the whole \code{chr} is shown.}

\item{end}{Optional end coordinate of the region passed to IGV. When both
\code{start} and \code{end} are \code{NULL}, the whole \code{chr} is shown.}

\item{out_dir}{Directory where IGV writes snapshot images.}

\item{format}{Snapshot output format used when \code{snapshot_name} is \code{NULL}.
Supported values are \code{"png"}, \code{"pdf"}, \code{"svg"}, \code{"jpg"}, and \code{"jpeg"}.}

\item{snapshot_name}{Output snapshot filename. The extension should be
match \code{format}. When \code{NULL}, a filename is generated from \code{chr}, \code{start},
\code{end}, and \code{format}.}

\item{igv}{Command or full path used to launch IGV. On Linux this is usually
\code{igv.sh}; on macOS command-line IGV installations also provide \code{igv.sh}.}

\item{batch_file}{Path where the temporary IGV batch script is written. When
\code{NULL}, a temporary \code{.igv} file is used.}

\item{sort}{Alignment sorting mode passed to IGV \code{sort}. Use \code{NULL} to skip
sorting.}

\item{display}{Alignment display mode. One of \code{"collapse"}, \code{"expand"},
\code{"squish"}, or \code{NULL}.}

\item{max_panel_height}{Optional value for IGV \code{maxPanelHeight}.}

\item{index}{Optional BAM index path. When supplied, the batch script uses
\verb{load bam index=index}.}

\item{auto_index_fasta}{Logical. If \code{TRUE}, create a missing FASTA \code{.fai}
index with \code{\link[Rsamtools:scanFa]{Rsamtools::indexFa()}} before writing the IGV batch script.}

\item{extra_commands}{Character vector of additional IGV batch commands added
after \code{load} and before \code{goto}.}

\item{use_xvfb}{Logical. If \code{TRUE}, run IGV through \code{xvfb-run -a}, useful on
headless Linux servers.}

\item{dry_run}{Logical. If \code{TRUE}, write and return the batch script and
command without launching IGV.}

\item{quiet}{Logical. If \code{TRUE}, suppress console output from FASTA indexing
and the IGV system command. Set to \code{FALSE} to show IGV logs while debugging.}

\item{overwrite}{Logical. If \code{FALSE}, stop when \code{batch_file} already exists.}
}
\value{
Invisibly returns a list containing the batch file path, batch
commands, system command, arguments, exit status, and expected snapshot path.
}
\description{
\code{igv_snapshot()} writes an IGV batch script for a reference FASTA, BAM file,
genomic region, and output snapshot, then optionally runs IGV from R.
}
\details{
IGV must be installed separately. Download IGV Desktop from
\url{https://igv.org/doc/desktop/DownloadPage/}. The command-line launcher is
documented at \url{https://igv.org/doc/desktop/UserGuide/advanced/command_line/}
and batch commands are documented at
\url{https://igv.org/doc/desktop/UserGuide/tools/batch/}. On Linux, the IGV
archive includes \code{igv.sh}; make sure it is on your \code{PATH}, or pass the full
path to \code{igv}.

On headless Linux servers, install a virtual framebuffer and use
\code{use_xvfb = TRUE}. For Debian/Ubuntu, this is usually:
\verb{sudo apt update && sudo apt install xvfb}. Conda users can install IGV and
Xvfb in an isolated environment, for example with:
\verb{mamba install -c bioconda igv -c conda-forge xorg-x11-server-Xvfb}.

BAM files should be coordinate sorted and indexed. If \code{index} is not supplied,
IGV will look for a standard index such as \code{sample.bam.bai}. The FASTA should
also have a \code{.fai} index. By default, \code{igv_snapshot()} creates a missing
FASTA index with \code{\link[Rsamtools:scanFa]{Rsamtools::indexFa()}}, the R equivalent of
\verb{samtools faidx ref.fasta}.
}
\examples{
ref <- tempfile(fileext = ".fasta")
bam <- tempfile(fileext = ".bam")
writeLines(c(">barcode09", "ACGTACGTACGT"), ref)
file.create(bam)
file.create(paste0(bam, ".bai"))

res <- igv_snapshot(
  genome_fasta = ref,
  bam = bam,
  chr = "barcode09",
  start = 1,
  end = 12,
  dry_run = TRUE
)
res$commands

}
