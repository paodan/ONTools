% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align_contigs_to_reference.R
\name{align_contigs_to_reference}
\alias{align_contigs_to_reference}
\title{Align assembled contigs to a reference and create BAM index}
\usage{
align_contigs_to_reference(
  reference,
  assembly,
  align_bam,
  preset = "asm5",
  threads = 16,
  secondary = FALSE,
  minimap2 = "minimap2",
  samtools = "samtools",
  conda_env = NULL,
  conda = "conda",
  dry_run = FALSE,
  echo = TRUE,
  stderr = ""
)
}
\arguments{
\item{reference}{Reference FASTA file.}

\item{assembly}{Assembled contig FASTA file, for example Flye
\code{assembly.fasta} or \code{draft_assembly.fasta}.}

\item{align_bam}{Output coordinate-sorted BAM file.}

\item{preset}{Minimap2 preset passed to \code{minimap2 -x}. Use \code{"asm5"} for very
similar assemblies, \code{"asm10"} for moderately diverged assemblies, or
\code{"asm20"} for more diverged assemblies.}

\item{threads}{Positive integer thread count passed to both \code{minimap2 -t}
and \verb{samtools sort -@}.}

\item{secondary}{Logical. If \code{FALSE}, pass \code{--secondary=no} to minimap2.}

\item{minimap2, samtools}{Command names or executable paths.}

\item{conda_env}{Optional conda environment name. If supplied, external
commands are run with \verb{conda run -n <conda_env>}.}

\item{conda}{Conda executable name or path used when \code{conda_env} is supplied.}

\item{dry_run}{Logical. If \code{TRUE}, return planned commands without running.}

\item{echo}{Logical. If \code{TRUE}, print planned commands before execution.}

\item{stderr}{Passed to \code{\link[=system2]{system2()}}. Defaults stream errors to the R console.}
}
\value{
Invisibly returns a list with \code{status}, \code{commands}, \code{paths},
\code{preset}, and \code{conda_env}.
}
\description{
\code{align_contigs_to_reference()} maps assembled contigs to a reference genome
or plasmid with \code{minimap2}, sorts the alignment with \verb{samtools sort}, and
creates a BAM index with \verb{samtools index}.
}
\examples{
ref <- tempfile(fileext = ".fasta")
assembly <- tempfile(fileext = ".fasta")
bam <- tempfile(fileext = ".bam")
writeLines(c(">ref", "ACGTACGT"), ref)
writeLines(c(">contig1", "ACGT"), assembly)
res <- align_contigs_to_reference(ref, assembly, bam, dry_run = TRUE)
res$commands$minimap2

}
