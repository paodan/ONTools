% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastq_stats.R
\name{fastq_stats}
\alias{fastq_stats}
\title{Summarize FASTQ files with optional length-filtered views}
\usage{
fastq_stats(
  fastq,
  min_lengths = NULL,
  include_original = TRUE,
  output_tsv = NULL,
  seqkit = "seqkit",
  conda_env = NULL,
  conda = "conda",
  dry_run = FALSE,
  echo = TRUE,
  stderr = ""
)
}
\arguments{
\item{fastq}{Character vector of input FASTQ or FASTQ.GZ files.}

\item{min_lengths}{Optional positive integer vector. For each value, run
\verb{seqkit seq -m <min_length> <fastq> | seqkit stats -a -T}.}

\item{include_original}{Logical. If \code{TRUE}, also run \code{seqkit stats -a} on the
unfiltered \code{fastq} files.}

\item{output_tsv}{Optional output TSV file for the combined stats table.}

\item{seqkit}{Command name or executable path for \code{seqkit}.}

\item{conda_env}{Optional conda environment name. If supplied, \code{seqkit}
commands are run with \verb{conda run -n <conda_env>}.}

\item{conda}{Conda executable name or path used when \code{conda_env} is supplied.}

\item{dry_run}{Logical. If \code{TRUE}, return planned commands without running.}

\item{echo}{Logical. If \code{TRUE}, print planned commands before execution.}

\item{stderr}{Passed to \code{\link[=system2]{system2()}}. Defaults stream errors to the R console.}
}
\value{
Invisibly returns a list with \code{status}, \code{stats}, \code{commands}, and
\code{paths}. \code{stats} is a data frame with normalized column names and common
numeric columns converted from \code{seqkit}'s display format. The \code{stats} data
frame contains:
\describe{
\item{stat_type}{Summary type. \code{"original"} means direct statistics on an
input FASTQ. \code{"min_length"} means statistics after temporary
\verb{seqkit seq -m <min_length>} filtering.}
\item{min_length}{Minimum read length used for a filtered summary. \code{NA} for
unfiltered original summaries.}
\item{source_fastq}{Original FASTQ file used to generate the row. For
filtered summaries this is the file piped through \code{seqkit seq}.}
\item{file}{File label reported by \code{seqkit stats}. For piped filtered
summaries this is usually \code{"stdin"}.}
\item{format}{Detected file format, for example \code{"FASTQ"}.}
\item{type}{Detected sequence alphabet, for example \code{"DNA"}, \code{"RNA"}, or
\code{"Protein"}.}
\item{num_seqs}{Number of sequences / reads.}
\item{sum_len}{Total number of bases across all reads.}
\item{min_len}{Shortest read length.}
\item{avg_len}{Mean read length.}
\item{max_len}{Longest read length.}
\item{q1, q2, q3}{First quartile, median, and third quartile of read
lengths, when reported by \code{seqkit}.}
\item{sum_gap}{Total number of gap characters, when reported.}
\item{n50, q20pct, q30pct}{N50 read length and percentages of bases with
Q20/Q30 quality, when reported by \code{seqkit stats -a}.}
\item{avg_qual}{Average base quality, when reported.}
}
Other columns may be present depending on the installed \code{seqkit} version.
}
\description{
\code{fastq_stats()} wraps \code{seqkit stats -a -T}. It can summarize one or more FASTQ
files directly and optionally summarize temporary \code{seqkit seq -m} filtered
views without writing filtered FASTQ files.
}
\examples{
reads <- tempfile(fileext = ".fastq.gz")
writeLines(c("@read1", "ACGT", "+", "!!!!"), reads)
res <- fastq_stats(reads, min_lengths = c(1000, 5000), dry_run = TRUE)
res$commands

}
