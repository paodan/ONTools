% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_fasta_to_ref.R
\name{map_fasta_to_ref}
\alias{map_fasta_to_ref}
\title{Map FASTA sequences to a reference}
\usage{
map_fasta_to_ref(
  fastaFile,
  ref,
  output_file = NULL,
  preset = "asm5",
  threads = 1,
  secondary = TRUE,
  minimap2 = "minimap2",
  samtools = "samtools",
  conda_env = NULL,
  conda = "conda",
  dry_run = FALSE,
  strict = FALSE,
  echo = TRUE,
  stderr = ""
)
}
\arguments{
\item{fastaFile}{Query FASTA file.}

\item{ref}{Reference FASTA file.}

\item{output_file}{Output BAM file. If \code{NULL}, a temporary \code{.bam} file is
created.}

\item{preset}{Minimap2 preset passed to \code{minimap2 -x}.}

\item{threads}{Positive integer thread count passed to \code{minimap2 -t} and
\verb{samtools sort -@}.}

\item{secondary}{Logical. If \code{FALSE}, pass \code{--secondary=no} to minimap2.}

\item{minimap2, samtools}{Command names or executable paths.}

\item{conda_env}{Optional conda environment name. If supplied, external
commands are run with \verb{conda run -n <conda_env>}.}

\item{conda}{Conda executable name or path used when \code{conda_env} is supplied.}

\item{dry_run}{Logical. If \code{TRUE}, return planned commands without running
external tools.}

\item{strict}{Logical. If \code{TRUE}, stop when minimap2 finishes but no PAF
alignment is found. If \code{FALSE}, return \code{NULL}, matching the original helper
behavior.}

\item{echo}{Logical. If \code{TRUE}, print planned commands before execution.}

\item{stderr}{Passed to \code{\link[=system2]{system2()}}.}
}
\value{
Invisibly returns a list with \code{status}, \code{commands}, \code{paths}, \code{paf},
\code{output_file}, \code{preset}, and \code{conda_env}.
}
\description{
\code{map_fasta_to_ref()} aligns query FASTA sequences to a reference FASTA with
\code{minimap2}, reads the PAF alignment table, then writes a coordinate-sorted
and indexed BAM with \code{samtools}.
}
\examples{
query <- tempfile(fileext = ".fasta")
ref <- tempfile(fileext = ".fasta")
writeLines(c(">query1", "ACGTACGT"), query)
writeLines(c(">chr1", "ACGTACGTACGT"), ref)
res <- map_fasta_to_ref(query, ref, dry_run = TRUE)
res$commands$minimap2_paf

}
