% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_species_pie.R
\name{plot_species_pie}
\alias{plot_species_pie}
\title{Plot a species pie chart from a wf-16s alignment table}
\usage{
plot_species_pie(
  alignment_table,
  showPercent = FALSE,
  otherPct = 0.01,
  title = NULL,
  value = NULL,
  species_col = NULL,
  reads_col = NULL,
  percent_col = NULL,
  top_n = NULL,
  other_label = "Others",
  label_slices = FALSE,
  label_min_pct = 0.03,
  output = NULL,
  width = 7,
  height = 6
)
}
\arguments{
\item{alignment_table}{Path to a wf-16s alignment stats TSV file, or a data
frame already read from such a file.}

\item{showPercent}{Logical. Backward-compatible shortcut. If \code{TRUE}, plot
values from \code{percent_col}; if \code{FALSE}, plot read counts from \code{reads_col}.
Ignored when \code{value} is supplied.}

\item{otherPct}{Relative abundance cutoff for grouping rare species into
\code{"Others"}. Values are fractions, so \code{0.01} means 1\%.}

\item{title}{Optional plot title.}

\item{value}{Plot value mode: \code{"reads"} uses read counts and \code{"percent"} uses
\code{percent_col}.}

\item{species_col}{Column containing species names. If \code{NULL}, common wf-16s
names such as \code{species}, \code{genuspecies}, and \code{genus species} are detected.}

\item{reads_col}{Column containing read counts. If \code{NULL}, common names such
as \code{number of reads} and \code{number.of.reads} are detected.}

\item{percent_col}{Column containing per-reference read percentage. If
\code{NULL}, common names such as \code{pcreads} and \code{pc reads} are detected.}

\item{top_n}{Optional maximum number of species to show before grouping the
rest into \code{"Others"}.}

\item{other_label}{Label used for grouped rare species.}

\item{label_slices}{Logical. If \code{TRUE}, add text labels to slices.}

\item{label_min_pct}{Minimum fraction required for a slice label.}

\item{output}{Optional output image path. When supplied, the plot is saved
with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{width, height}{Plot width and height in inches when \code{output} is supplied.}
}
\value{
A ggplot object. The aggregated plotting data is available from
\code{attr(plot, "plot_data")}.
}
\description{
\code{plot_species_pie()} reads one \code{barcode*-alignment-stats.tsv} table generated
by wf-16s and draws a pie chart of species composition. Low-abundance species
can be grouped into an \code{"Others"} slice.
}
