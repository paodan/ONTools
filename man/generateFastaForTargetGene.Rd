% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generateFastaForTargetGene.R
\name{generateFastaForTargetGene}
\alias{generateFastaForTargetGene}
\title{Generate target-gene and replacement FASTA fragments}
\usage{
generateFastaForTargetGene(
  output_path,
  genome_fasta = "genome/GCA_019096115.1_ASM1909611v1_genomic.fna",
  gene_seq_fasta = "fasta/v1/3_GME292_g.fasta",
  replace_seq_fastq = "fasta/v1/3_NTC.fa",
  h1_len = 1000,
  h2_len = 1000,
  h1up_len = 1000,
  h2down_len = 1000,
  preset = "asm5",
  min_mapq = 0,
  max_unaligned = 0,
  paf_file = NULL,
  minimap2 = "minimap2",
  conda_env = NULL,
  conda = "conda",
  output_ext = ".fasta",
  combined_fasta_name = "all_sequences.fasta",
  trim_to_bounds = FALSE,
  dry_run = FALSE,
  echo = TRUE,
  stderr = ""
)
}
\arguments{
\item{output_path}{Output directory. One subdirectory is created for each
accepted mapping hit.}

\item{genome_fasta}{Genome/reference FASTA file.}

\item{gene_seq_fasta}{FASTA file containing exactly one target gene sequence.}

\item{replace_seq_fastq}{FASTA file containing exactly one replacement
sequence. The argument name is kept for backward compatibility.}

\item{h1_len, h2_len}{Homology-arm lengths upstream and downstream of the
target gene.}

\item{h1up_len, h2down_len}{Additional flank lengths outside H1 and H2.}

\item{preset}{Minimap2 preset used when aligning the gene to the genome.}

\item{min_mapq}{Minimum mapping quality for accepted PAF hits.}

\item{max_unaligned}{Maximum allowed number of unaligned query bases across
both ends. The default \code{0} keeps only full-length gene alignments.}

\item{paf_file}{Optional existing PAF file. If supplied, minimap2 is not run.}

\item{minimap2}{Command name or executable path.}

\item{conda_env}{Optional conda environment name. If supplied, minimap2 is run
with \verb{conda run -n <conda_env>}.}

\item{conda}{Conda executable name or path used when \code{conda_env} is supplied.}

\item{output_ext}{Extension appended to generated FASTA file names.}

\item{combined_fasta_name}{File name for the combined FASTA written in each
hit directory. Set to \code{NULL} to skip writing the combined FASTA.}

\item{trim_to_bounds}{Logical. If \code{FALSE}, stop when requested flank regions
extend outside the contig. If \code{TRUE}, trim regions to contig bounds.}

\item{dry_run}{Logical. If \code{TRUE}, return planned commands without running
minimap2 or writing FASTA files.}

\item{echo}{Logical. If \code{TRUE}, print planned commands before execution.}

\item{stderr}{Passed to \code{\link[=system2]{system2()}}.}
}
\value{
Invisibly returns a list with \code{paf}, \code{seq}, \code{output_path},
\code{written_files}, \code{commands}, and \code{paths}.
}
\description{
\code{generateFastaForTargetGene()} maps one gene sequence to a genome and writes
FASTA files for the gene, homology arms, flanking regions, and replacement
constructs.
}
\examples{
\dontrun{
res <- generateFastaForTargetGene(
  output_path = "./fasta/v2",
  genome_fasta = "genome/GCA_019096115.1_ASM1909611v1_genomic.fna",
  gene_seq_fasta = "fasta/v1/3_GME292_g.fasta",
  replace_seq_fastq = "fasta/v1/3_NTC.fa",
  h1_len = 1000,
  h2_len = 1000,
  h1up_len = 1000,
  h2down_len = 1000
)
}

}
