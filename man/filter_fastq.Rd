% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_fastq.R
\name{filter_fastq}
\alias{filter_fastq}
\alias{filter_fastq_by_length}
\title{Filter or transform FASTQ records with seqkit seq}
\usage{
filter_fastq(
  fastq,
  output_fastq,
  min_length = NULL,
  max_length = NULL,
  min_quality = NULL,
  max_quality = NULL,
  reverse = FALSE,
  complement = FALSE,
  remove_gaps = FALSE,
  gap_letters = NULL,
  upper_case = FALSE,
  lower_case = FALSE,
  dna2rna = FALSE,
  rna2dna = FALSE,
  only_id = FALSE,
  name = FALSE,
  seq = FALSE,
  qual = FALSE,
  validate_seq = FALSE,
  line_width = NULL,
  seq_type = NULL,
  threads = NULL,
  compress_level = NULL,
  extra_args = NULL,
  seqkit = "seqkit",
  conda_env = NULL,
  conda = "conda",
  dry_run = FALSE,
  echo = TRUE,
  stderr = ""
)

filter_fastq_by_length(
  fastq,
  output_fastq,
  min_length = 5000,
  seqkit = "seqkit",
  conda_env = NULL,
  conda = "conda",
  dry_run = FALSE,
  echo = TRUE,
  stderr = ""
)
}
\arguments{
\item{fastq}{Input FASTQ or FASTQ.GZ file.}

\item{output_fastq}{Output FASTQ or FASTQ.GZ file.}

\item{min_length, max_length}{Minimum and maximum read length passed to
\code{seqkit seq -m/-M}. Use \code{NULL} for no limit.}

\item{min_quality, max_quality}{Minimum and maximum average quality passed to
\code{seqkit seq -Q/-R}. Use \code{NULL} for no limit.}

\item{reverse, complement}{Logical. Pass \code{--reverse} or \code{--complement}.}

\item{remove_gaps}{Logical. Pass \code{--remove-gaps}.}

\item{gap_letters}{Gap letters passed to \code{--gap-letters} when non-\code{NULL}.}

\item{upper_case, lower_case}{Logical. Pass \code{--upper-case} or \code{--lower-case}.}

\item{dna2rna, rna2dna}{Logical. Pass \code{--dna2rna} or \code{--rna2dna}.}

\item{only_id, name, seq, qual}{Logical. Output only IDs, names, sequences, or
qualities with \code{--only-id}, \code{--name}, \code{--seq}, or \code{--qual}.}

\item{validate_seq}{Logical. Pass \code{--validate-seq}.}

\item{line_width}{Optional line width passed to \code{--line-width}.}

\item{seq_type}{Optional sequence type passed to \code{--seq-type}.}

\item{threads}{Optional thread count passed to \code{--threads}.}

\item{compress_level}{Optional compression level passed to \code{--compress-level}.}

\item{extra_args}{Optional character vector of additional raw \code{seqkit seq}
arguments appended before the input FASTQ.}

\item{seqkit}{Command name or executable path for \code{seqkit}.}

\item{conda_env}{Optional conda environment name. If supplied, \code{seqkit} is run
with \verb{conda run -n <conda_env>}.}

\item{conda}{Conda executable name or path used when \code{conda_env} is supplied.}

\item{dry_run}{Logical. If \code{TRUE}, return the planned command without
running it.}

\item{echo}{Logical. If \code{TRUE}, print the planned command before execution.}

\item{stderr}{Passed to \code{\link[=system2]{system2()}}. Defaults stream errors to the R console.}
}
\value{
Invisibly returns a list with \code{status}, \code{command}, \code{args},
\code{command_string}, and \code{paths}.
}
\description{
\code{filter_fastq()} wraps \code{seqkit seq} to filter reads by length or average
quality and optionally apply common sequence transformations.
}
\examples{
reads <- tempfile(fileext = ".fastq.gz")
out <- tempfile(fileext = ".fastq")
writeLines(c("@read1", "ACGT", "+", "!!!!"), reads)
res <- filter_fastq(reads, out, min_length = 5000, dry_run = TRUE)
res$command_string

}
