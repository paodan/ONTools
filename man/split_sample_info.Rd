% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_sample_info.R
\name{split_sample_info}
\alias{split_sample_info}
\title{Split sample information by project and expected amplicon size}
\usage{
split_sample_info(
  sampleInfo,
  by = c("Project_ID", "Expected_Size_bp"),
  keep = c("Sample_ID", "Project_ID", "Barcode_ID", "Sample_Type", "Expected_Size_bp",
    "Min_Read_Length", "Max_Read_Length", "Gel_lane", "Gel_Quality", "Clear_Band",
    "Analysis_Type", "Primer_F", "Primer_R"),
  sheet = "2_metadata",
  skip = NULL,
  min_read_delta = 300,
  max_read_delta = 200,
  output_dir = NULL,
  na_on_size_error = TRUE
)
}
\arguments{
\item{sampleInfo}{Sample information table, either a data frame or an
existing file path. CSV files are read when \code{sheet = NULL}; otherwise the
file is treated as an Excel workbook and read with \code{readxl}.}

\item{by}{Character vector of columns used to create the \code{Folders} group
name. Values are sanitized with the same path-safe naming used by
\code{\link[=move_fastq_to_folders]{move_fastq_to_folders()}}.}

\item{keep}{Character vector of columns to keep in the returned and saved
sample information tables. \code{Folders} is always kept.}

\item{sheet}{Excel sheet name. Set to \code{NULL} to read \code{sampleInfo} as CSV.}

\item{skip}{Number of rows to skip before reading the table. When reading an
Excel sheet and \code{skip = NULL}, the function looks for a \verb{[data]} marker in
the first column and uses the next row as the table header.}

\item{min_read_delta}{Non-negative number subtracted from \code{Expected_Size_bp}
to fill missing \code{Min_Read_Length} values. Defaults to \code{300}.}

\item{max_read_delta}{Non-negative number added to \code{Expected_Size_bp} to fill
missing \code{Max_Read_Length} values. Defaults to \code{200}.}

\item{output_dir}{Optional directory where one CSV file per \code{Folders} group
is written. When \code{NULL}, no files are written.}

\item{na_on_size_error}{Logical. Passed to \code{\link[=parse_size]{parse_size()}}. If \code{TRUE}, an
unparseable \code{Expected_Size_bp} value leaves the derived read-length limits
as \code{NA} instead of throwing an error.}
}
\value{
A list with two elements:
\describe{
\item{\code{data}}{A named list of data frames split by \code{Folders}.}
\item{\code{files}}{A named list of CSV file paths, or an empty list when
\code{output_dir = NULL}.}
}
}
\description{
\code{split_sample_info()} reads a sample information table, creates a \code{Folders}
grouping column from selected metadata columns, fills missing read-length
filter columns from the expected amplicon size, and optionally writes one CSV
file per group.
}
\examples{
sample_info <- data.frame(
  Sample_ID = c("S1", "S2"),
  Project_ID = c("P1", "P1"),
  Barcode_ID = c("NB01", "NB02"),
  Expected_Size_bp = c("1000bp", "1500bp")
)

split_sample_info(sample_info)

}
