% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_deletion_event.R
\name{summarize_deletion_event}
\alias{summarize_deletion_event}
\title{Summarize support for a deletion event from BAM CIGAR strings}
\usage{
summarize_deletion_event(
  bam,
  reference_name,
  deletion_start,
  deletion_end,
  breakpoint_tolerance = 5,
  match_mode = c("breakpoints", "contains", "overlap"),
  min_overlap_fraction = 0.8,
  min_mapq = 20,
  min_flank_coverage = 20,
  max_wt_deletion_bases = 0,
  include_read_table = FALSE
)
}
\arguments{
\item{bam}{Coordinate-sorted BAM file. A BAM index is recommended.}

\item{reference_name}{Reference/contig name containing the expected deletion.}

\item{deletion_start, deletion_end}{One-based inclusive reference coordinates
of the expected deleted interval.}

\item{breakpoint_tolerance}{Maximum allowed distance in bp between observed
and expected deletion boundaries for a read to count as \code{DEL_SUPPORT}.}

\item{match_mode}{How an observed deletion should match the requested
interval. \code{"breakpoints"} requires both deletion breakpoints to fall within
\code{breakpoint_tolerance}. \code{"contains"} counts a read as deletion support when
the observed deletion contains the requested interval, allowing
\code{breakpoint_tolerance} on both sides. \code{"overlap"} counts any observed
deletion overlapping the requested interval by at least
\code{min_overlap_fraction} of the requested interval.}

\item{min_overlap_fraction}{Minimum fraction of the requested deletion
interval that must overlap an observed deletion when
\code{match_mode = "overlap"}.}

\item{min_mapq}{Minimum read mapping quality.}

\item{min_flank_coverage}{Number of aligned reference bases required on both
sides of the deletion interval for a read to be informative. Near reference
boundaries, the available flank length is used.}

\item{max_wt_deletion_bases}{Maximum number of deleted/skipped bases allowed
inside the expected deletion interval for a read to count as \code{WT_SUPPORT}.}

\item{include_read_table}{Logical. If \code{TRUE}, include per-read classifications
and observed deletion coordinates in the return value.}
}
\value{
A list with \code{summary}, \code{read_table}, and \code{parameters}. \code{summary}
contains read counts and percentages for \code{DEL_SUPPORT}, \code{WT_SUPPORT}, and
\code{AMBIGUOUS}.
}
\description{
\code{summarize_deletion_event()} counts read support for a user-specified
deletion interval directly from BAM alignments. It is intended for gene
editing results where a deletion event is visually clear in IGV/BAM but may
be awkwardly represented in a VCF.
}
\examples{
# A real indexed BAM is needed for execution.
# summarize_deletion_event(
#   bam = "sample.sort.bam",
#   reference_name = "PLA3_B_",
#   deletion_start = 100,
#   deletion_end = 160
# )

}
