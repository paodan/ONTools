% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_filtlong.R
\name{run_filtlong}
\alias{run_filtlong}
\title{Filter long reads with Filtlong}
\usage{
run_filtlong(
  reads,
  output_fastq,
  target_bases = NULL,
  keep_percent = NULL,
  min_length = NULL,
  min_mean_q = NULL,
  min_window_q = NULL,
  length_weight = NULL,
  mean_q_weight = NULL,
  window_q_weight = NULL,
  window_size = NULL,
  trim = FALSE,
  split = NULL,
  illumina_1 = NULL,
  illumina_2 = NULL,
  assembly = NULL,
  verbose = FALSE,
  extra_args = NULL,
  filtlong = "filtlong",
  conda_env = NULL,
  conda = "conda",
  dry_run = FALSE,
  echo = TRUE,
  stderr = ""
)
}
\arguments{
\item{reads}{Input FASTQ or FASTQ.GZ file.}

\item{output_fastq}{Output FASTQ file. Use a \code{.gz} suffix only if your
downstream workflow or shell environment compresses it separately; Filtlong
writes FASTQ text to standard output.}

\item{target_bases}{Optional target number of bases to keep, passed to
\code{filtlong --target_bases}.}

\item{keep_percent}{Optional percentage of the best reads to keep, passed to
\code{filtlong --keep_percent}.}

\item{min_length}{Optional minimum read length, passed to
\code{filtlong --min_length}.}

\item{min_mean_q}{Optional minimum mean read quality, passed to
\code{filtlong --min_mean_q}.}

\item{min_window_q}{Optional minimum window quality, passed to
\code{filtlong --min_window_q}.}

\item{length_weight}{Optional weight for read length in read scoring, passed
to \code{filtlong --length_weight}.}

\item{mean_q_weight}{Optional weight for mean quality in read scoring, passed
to \code{filtlong --mean_q_weight}.}

\item{window_q_weight}{Optional weight for window quality in read scoring,
passed to \code{filtlong --window_q_weight}.}

\item{window_size}{Optional window size used for window-quality scoring,
passed to \code{filtlong --window_size}.}

\item{trim}{Logical. If \code{TRUE}, pass \code{filtlong --trim} to trim low-quality
read ends when supported by the installed Filtlong version.}

\item{split}{Optional integer passed to \code{filtlong --split} to split reads at
poor-quality positions when supported by the installed Filtlong version.}

\item{illumina_1, illumina_2}{Optional paired short-read files passed to
\code{filtlong --illumina_1} and \code{filtlong --illumina_2} for external quality
scoring.}

\item{assembly}{Optional reference assembly passed to \code{filtlong --assembly}
for external quality scoring.}

\item{verbose}{Logical. If \code{TRUE}, pass \code{filtlong --verbose}.}

\item{extra_args}{Optional character vector of additional raw Filtlong
arguments appended before the input reads.}

\item{filtlong}{Command name or executable path for \code{filtlong}.}

\item{conda_env}{Optional conda environment name. If supplied, Filtlong is run
with \verb{conda run -n <conda_env>}.}

\item{conda}{Conda executable name or path used when \code{conda_env} is supplied.}

\item{dry_run}{Logical. If \code{TRUE}, return the planned command without
running it.}

\item{echo}{Logical. If \code{TRUE}, print the planned command before execution.}

\item{stderr}{Passed to \code{\link[=system2]{system2()}}. Defaults stream errors to the R console.}
}
\value{
Invisibly returns a list with \code{status}, \code{command}, \code{args},
\code{command_string}, \code{paths}, and \code{conda_env}.
}
\description{
\code{run_filtlong()} wraps \code{filtlong} to keep the best long reads by length,
quality, and optional target yield.
}
\examples{
reads <- tempfile(fileext = ".fastq")
out <- tempfile(fileext = ".fastq")
writeLines(c("@read1", "ACGT", "+", "!!!!"), reads)
res <- run_filtlong(
  reads = reads,
  output_fastq = out,
  target_bases = 80000000,
  dry_run = TRUE
)
res$command_string

}
