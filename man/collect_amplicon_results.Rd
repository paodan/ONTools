% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect_amplicon_results.R
\name{collect_amplicon_results}
\alias{collect_amplicon_results}
\title{Collect amplicon workflow deliverables}
\usage{
collect_amplicon_results(
  result_dir,
  output_dir,
  sample_map = NULL,
  consensus_file = "all-consensus-seqs.fasta",
  consensus_index_file = paste0(consensus_file, ".fai"),
  trimmed_consensus_file = "all-consensus-seqs_trimmed.fasta",
  barcode_pattern = "^barcode[0-9]+$",
  include_execution = FALSE,
  include_reports = TRUE,
  readme_name = "README.txt",
  overwrite = TRUE
)
}
\arguments{
\item{result_dir}{Path to a wf_amplicon_denovo result directory.}

\item{output_dir}{Path to the final-results directory to create.}

\item{sample_map}{Optional path to a barcode-to-sample mapping file. The file
is copied into \code{output_dir}.}

\item{consensus_file}{Name of the untrimmed consensus FASTA in \code{result_dir}.}

\item{consensus_index_file}{Name of the FASTA index for \code{consensus_file}.}

\item{trimmed_consensus_file}{Name of the trimmed consensus FASTA in
\code{result_dir}. The generated README recommends this file for downstream use.}

\item{barcode_pattern}{Regular expression used to identify barcode
directories.}

\item{include_execution}{Logical. If \code{TRUE}, copy the \code{execution} directory
when present.}

\item{include_reports}{Logical. If \code{TRUE}, copy common workflow report files
such as \code{wf-amplicon-report.html}, \code{params.json}, and \code{versions.txt} when
present.}

\item{readme_name}{README filename written into \code{output_dir}.}

\item{overwrite}{Logical. If \code{TRUE}, replace existing files in \code{output_dir}.}
}
\value{
Invisibly returns a data frame listing attempted copy operations,
source paths, destination paths, item type, and whether each source existed.
}
\description{
\code{collect_amplicon_results()} collects the main deliverables from a dorado /
wf_amplicon_denovo result directory into a clean final-results directory. It
copies consensus FASTA files, barcode result directories, a barcode-to-sample
map, and writes a README explaining the files.
}
\examples{
result_dir <- tempfile("amplicon-result-")
output_dir <- tempfile("amplicon-final-")
dir.create(result_dir)
dir.create(file.path(result_dir, "barcode01", "alignments"), recursive = TRUE)
writeLines(">barcode01\nACGT", file.path(result_dir, "all-consensus-seqs.fasta"))
writeLines("barcode01\t4\t0\t4\t5", file.path(result_dir, "all-consensus-seqs.fasta.fai"))
writeLines(">barcode01\nACGT", file.path(result_dir, "all-consensus-seqs_trimmed.fasta"))
writeLines("bam", file.path(result_dir, "barcode01", "alignments", "barcode01.bam"))
sample_map <- file.path(result_dir, "sample_map.tsv")
writeLines("barcode\tsample\nbarcode01\tsampleA", sample_map)

collect_amplicon_results(result_dir, output_dir, sample_map = sample_map)

}
