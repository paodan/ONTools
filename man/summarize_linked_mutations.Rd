% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_linked_mutations.R
\name{summarize_linked_mutations}
\alias{summarize_linked_mutations}
\title{Summarize linked mutation frequencies from a BAM file}
\usage{
summarize_linked_mutations(
  bam,
  reference_fasta = NULL,
  region = NULL,
  variants,
  min_mapq = 20,
  min_baseq = 10,
  require_complete = TRUE,
  include_read_table = FALSE,
  no_call_label = "NO_CALL"
)
}
\arguments{
\item{bam}{Coordinate-sorted BAM file. A BAM index is recommended.}

\item{reference_fasta}{Optional reference FASTA. It is currently used only
for input provenance in the return value; candidate \code{REF} alleles are taken
from \code{variants}.}

\item{region}{Optional region string such as \code{"chr1"} or \code{"chr1:100-250"}.
Reads outside this region are ignored. Variants are also restricted to this
region when supplied.}

\item{variants}{Candidate variants as a data frame or VCF path. Data frames
must contain \code{CHROM}, \code{POS}, \code{REF}, and \code{ALT} columns. VCF paths are read
with \code{\link[=read_vcf]{read_vcf()}}. Multi-allelic \code{ALT} values are split into separate
candidate variants.}

\item{min_mapq}{Minimum read mapping quality.}

\item{min_baseq}{Minimum base quality required for bases used to classify
SNP/MNP and anchor bases for INDELs.}

\item{require_complete}{Logical. If \code{TRUE}, haplotype frequencies use only
reads that can be classified at every candidate variant. If \code{FALSE}, reads
with \code{NO_CALL} at one or more sites are retained in the denominator.}

\item{include_read_table}{Logical. If \code{TRUE}, include one row per read in the
return value.}

\item{no_call_label}{Label for variants not covered or not classifiable in a
read.}
}
\value{
A list with:
\itemize{
\item \code{haplotypes}: haplotype counts and frequencies.
\item \code{read_table}: optional read-level classifications.
\item \code{variants}: normalized candidate variants.
\item \code{summary}: input and read-count summary.
}
}
\description{
\code{summarize_linked_mutations()} counts read-level mutation patterns across a
set of candidate variants. Candidate variants define the sites to inspect;
the BAM determines which variants co-occur on the same read.
}
\examples{
variants <- data.frame(
  CHROM = "chr1",
  POS = c(10, 20),
  REF = c("A", "C"),
  ALT = c("G", "T")
)
# A real indexed BAM is needed for execution.
# summarize_linked_mutations("sample.bam", variants = variants)

}
