% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_dorado_demux_to_fastq.R
\name{run_dorado_demux_to_fastq}
\alias{run_dorado_demux_to_fastq}
\title{Run Dorado demultiplexing and FASTQ conversion}
\usage{
run_dorado_demux_to_fastq(
  proj,
  kit_name = "EXP-NBD196",
  model = "sup",
  demux_out = NULL,
  fastq_out = "fastq_pass_trim",
  command = "run_dorado_demux_to_fastq",
  dry_run = FALSE,
  echo = TRUE,
  wait = TRUE,
  stdout = "",
  stderr = ""
)
}
\arguments{
\item{proj}{Project directory containing a \verb{pod5/} subdirectory.}

\item{kit_name}{Dorado barcode kit name passed to \code{--kit-name}.}

\item{model}{Dorado basecalling model or model alias passed to \code{--model}.}

\item{demux_out}{Demux output directory name under \code{proj}. If \code{NULL}, the
shell script uses its default: \verb{demux_out_<kit_name>}.}

\item{fastq_out}{FASTQ output directory name created inside each
Dorado-generated run directory.}

\item{command}{Shell command or executable path for \code{run_dorado_demux_to_fastq}.}

\item{dry_run}{Logical. If \code{TRUE}, return the command without running it.}

\item{echo}{Logical. If \code{TRUE}, print the command before execution.}

\item{wait}{Logical. Passed to \code{\link[=system2]{system2()}}. Use \code{FALSE} to launch the command
asynchronously.}

\item{stdout, stderr}{Passed to \code{\link[=system2]{system2()}}. Defaults stream output to the R
console.}
}
\value{
Invisibly returns a list with \code{command}, \code{args}, \code{status}, and
\code{paths}. \code{paths} contains the project directory, \verb{pod5/}, \verb{bam/}, generated
calls BAM path, demux output directory, and, after a completed run, any
discovered Dorado run directories, \verb{bam_pass/} directories, and FASTQ output
directories. In \code{dry_run = TRUE}, \code{status} is \code{NA_integer_}. With
\code{wait = FALSE}, \code{status} is the return value from \code{\link[=system2]{system2()}} and dynamic
output directories may not exist yet.
}
\description{
\code{run_dorado_demux_to_fastq()} is an R wrapper around the shell command
\code{run_dorado_demux_to_fastq}. It basecalls a project \verb{pod5/} directory,
demultiplexes the generated BAM, and converts demultiplexed BAM files to
\code{fastq.gz} files while preserving the Dorado barcode directory structure.
}
\details{
The wrapped shell script intentionally stops after \code{fastq.gz} generation.
Downstream analyses that depend on project-specific amplicon lengths or
filtering thresholds should be run separately on the generated FASTQ folders.
}
\examples{
proj <- tempfile("dorado-project-")
dir.create(file.path(proj, "pod5"), recursive = TRUE)

cmd <- run_dorado_demux_to_fastq(
  proj = proj,
  kit_name = "EXP-NBD196",
  model = "sup",
  dry_run = TRUE
)
cmd

}
