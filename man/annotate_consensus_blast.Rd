% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_consensus_blast.R
\name{annotate_consensus_blast}
\alias{annotate_consensus_blast}
\title{Annotate consensus sequences with BLASTN}
\usage{
annotate_consensus_blast(
  consensus_fasta,
  db = NULL,
  db_fasta = NULL,
  out_dir = "work/consensus_annotation",
  output_tsv = NULL,
  prefix = NULL,
  threads = 10,
  max_target_seqs = 20,
  evalue = "1e-20",
  task = NULL,
  word_size = NULL,
  strand = NULL,
  dust = NULL,
  perc_identity = NULL,
  extra_args = NULL,
  species_identity = 98.5,
  genus_identity = 95,
  family_identity = 90,
  min_query_coverage = 80,
  min_reference_coverage = 50,
  novel_identity = 97,
  blastn = "blastn",
  makeblastdb = "makeblastdb",
  conda_env = NULL,
  conda = "conda",
  dry_run = FALSE,
  echo = TRUE,
  stdout = "",
  stderr = ""
)
}
\arguments{
\item{consensus_fasta}{Query consensus/representative sequence FASTA file.}

\item{db}{BLAST database prefix passed to \code{blastn -db}. If \code{NULL},
\code{db_fasta} must be supplied and a database is built under \code{out_dir}.}

\item{db_fasta}{Optional FASTA file used to build a BLAST nucleotide database
with \code{makeblastdb}.}

\item{out_dir}{Output directory for BLAST results and optional database
files.}

\item{output_tsv}{Output BLAST TSV path. If \code{NULL}, writes
\verb{<out_dir>/<prefix>.blast.tsv}.}

\item{prefix}{Prefix used when constructing default output paths.}

\item{threads}{Positive integer thread count passed to \code{blastn}.}

\item{max_target_seqs}{Maximum number of target sequences reported per query.}

\item{evalue}{E-value cutoff passed to \code{blastn}.}

\item{task}{Optional BLASTN task, for example \code{"blastn"}, \code{"megablast"}, or
\code{"dc-megablast"}.}

\item{word_size}{Optional word size passed to \code{blastn -word_size}.}

\item{strand}{Optional strand passed to \code{blastn -strand}, for example
\code{"both"}, \code{"plus"}, or \code{"minus"}.}

\item{dust}{Optional dust setting passed to \code{blastn -dust}, for example
\code{"yes"} or \code{"no"}.}

\item{perc_identity}{Optional minimum percent identity passed to
\code{blastn -perc_identity}.}

\item{extra_args}{Optional additional \code{blastn} arguments as a character
vector, for example \code{c("-ungapped")}.}

\item{species_identity, genus_identity, family_identity}{Percent identity
cutoffs used for the conservative \code{annotation_level} column.}

\item{min_query_coverage, min_reference_coverage}{Minimum coverage cutoffs
used before assigning species/genus/family levels.}

\item{novel_identity}{Percent identity cutoff used to flag
\code{novel_candidate} when coverage is sufficient but the best hit is below
this identity.}

\item{blastn, makeblastdb}{Command names or executable paths.}

\item{conda_env}{Optional conda environment name. If supplied, external
commands are run with \code{conda run -n <conda_env>}.}

\item{conda}{Conda executable name or path used when \code{conda_env} is supplied.}

\item{dry_run}{Logical. If \code{TRUE}, return planned commands without running.}

\item{echo}{Logical. If \code{TRUE}, print planned commands before execution.}

\item{stdout, stderr}{Passed to \code{\link[base:system2]{system2()}}. Defaults stream command output
and errors to the R console.}
}
\value{
Invisibly returns a list with \code{status}, \code{commands}, \code{paths}, \code{blast},
\code{top_hits}, \code{thresholds}, and \code{conda_env}.
}
\description{
\code{annotate_consensus_blast()} runs \code{blastn} against a local nucleotide BLAST
database, parses the tabular result, calculates query/reference coverage,
extracts common UNITE-style taxonomy fields when present in subject titles,
and assigns a conservative annotation level for each hit.
}
\examples{
query <- tempfile(fileext = ".fasta")
writeLines(c(">consensus1", "ACGTACGT"), query)
res <- annotate_consensus_blast(
  consensus_fasta = query,
  db = "unite_eukaryotes",
  dry_run = TRUE
)
res$commands$blastn
}
