% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trim_fasta_keep_primers.R
\name{trim_fasta_keep_primers}
\alias{trim_fasta_keep_primers}
\title{Trim FASTA sequences while keeping primer sequences}
\usage{
trim_fasta_keep_primers(
  input_fasta,
  output_fasta,
  f_primer,
  r_primer,
  max_mismatch = 2,
  orient_to_forward = TRUE,
  width = 80
)
}
\arguments{
\item{input_fasta}{Path to an input FASTA file.}

\item{output_fasta}{Path to the output FASTA file.}

\item{f_primer}{Forward primer sequence. IUPAC ambiguity codes are supported.}

\item{r_primer}{Reverse primer sequence. IUPAC ambiguity codes are supported.}

\item{max_mismatch}{Maximum mismatches allowed when matching each primer.}

\item{orient_to_forward}{Logical. If \code{TRUE}, sequences matched in
reverse-complement orientation are reverse-complemented before writing.}

\item{width}{Number of bases per FASTA line in the output file.}
}
\value{
Invisibly returns a data frame with one row per input sequence:
sequence name, whether trimming succeeded, detected orientation, trim start
and end coordinates in the input sequence, input length, output length, and
output name written to the FASTA file.
}
\description{
\code{trim_fasta_keep_primers()} trims extra bases outside a primer-bounded
amplicon region. The forward-orientation structure is expected to be
\verb{extra + forward primer + insert + reverse-complement reverse primer + extra}.
The reverse-complement structure is also checked, and trimmed sequences can
optionally be returned in forward orientation.
}
\examples{
f_primer <- "AGAGTTTGATCMTGGCTCAG"
r_primer <- "TACGGYTACCTTGTTACGACTT"
input <- tempfile(fileext = ".fasta")
output <- tempfile(fileext = ".fasta")
seq <- paste0(
  "AA",
  "AGAGTTTGATCATGGCTCAG",
  "ACGTACGT",
  "AAGTCGTAACAAGGTACCGTA",
  "TT"
)
Biostrings::writeXStringSet(Biostrings::DNAStringSet(c(sample1 = seq)), input)

trim_fasta_keep_primers(
  input_fasta = input,
  output_fasta = output,
  f_primer = f_primer,
  r_primer = r_primer
)

}
